# Mission Fixtures

Deterministic mission graph fixtures for phase, decision point, Command Decision, and outcome-flag contract tests.

Versioned mission-definition fixtures live in version-specific subdirectories so the legacy flat-directory graph validator cannot mistake a definition for a graph scenario.

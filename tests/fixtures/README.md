# Test Fixtures

Small deterministic test fixtures for retrieval, mission graph, and future Director contract tests.

Fixtures should be purpose-built and should not duplicate full package data unless the test needs it.

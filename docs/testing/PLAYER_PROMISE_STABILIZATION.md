# Player promise stabilization evidence

Status: in progress. No full stabilization or live-soak completion claim.

Current checkpoint: branch-history containment is published as `7c274d161`, and staff-policy, terminal-pacing, and full-assistant-source fixes as `ebb010aea`. Both slices passed independent review and their final 227-check gates. Through request 231, totals are 186 real provider request events and nineteen distinct accepted source pairs plus one repeated-source replay. Batch 16 confirms repaired input packets but ends without acceptance because of model validation and output-limit failures; accepted state remains unchanged. The minimum twenty accepted pairs across three continued timelines is unmet. Full chronological branch reconstruction, successful staff progression, a correction-compatible Hesperus trigger, later campaign journeys, long-run results, and broader recovery proof remain open.

## Scope and baseline

The active goal covers objective authority, accepted-choice persistence, character information, campaign fairness, long-campaign behavior, and provider/host recovery. The execution plan is `docs/superpowers/plans/2026-09-14-player-promise-stabilization.md`.

- Published baseline verified through GitHub CLI: `1d4102db9695fcb90cc9976a5117267a8b255407`.
- Isolated worktree: `F:/git/Directive/.worktrees/player-promise-stabilization`.
- Existing personal checkout and prior test-account data are preserved.
- Existing localhost port 8000 refused a connection during setup; this is a host-availability observation, not a Directive defect.
- Baseline gate: all 225 checks passed; full output in ignored `artifacts/stabilization-20260914/baseline-gate.log`.
- Disposable SillyTavern 1.18.0, source `61ad2189fb53aa01c774ab03b70d7c47d772c640`, loopback port 8014, separate data root under ignored artifacts. Personal settings, installs, and saves were not changed.
- All 666 installed production files matched baseline hashes before live play. Subsequent candidate installs have a separate hash manifest; they are unpublished working-tree builds.
- NanoGPT profiles: utility `z-ai/glm-5.3-flash`; reasoning/narration `z-ai/glm-5.2:thinking`. Analysis max output 8192; opening narration 2200. Only necessary configuration was copied from an existing disposable account into a fresh account.

## Coverage ledger

| Area | Controlled evidence | Live evidence | Remaining |
| --- | --- | --- | --- |
| Objective interpretation and authority | Ten objective suites pass; combined production-path probe preserves unrelated facts and partial progress across failed persistence, stale interpretation, correction, and fresh hydration | Real opening completed; native Mission resolve/reopen persisted across page reload and explicit campaign reopen | Natural partial-progress interpretation and continued narration, rewards/transition journey |
| Choice/persistence/timelines | Branch/storage suites and retained-tail regression pass | Selected swipe accepted with exact source hash; real tail branch and reload preserve receipts, alternatives, time, and parent checkpoint | Truncating branches, edits/deletions, interrupted persistence, stale sessions |
| Information/spoilers | Nine-case extraction corpus; authored guide predicates and exact-ID retention covered | Conditional proposal withheld access; accepted private delivery grants Cross only; late arrival remains outside access until explicit delivery; concealed document line stays private; old meaning persists until explicit revision | Secondhand claims and broader UI adjudication; player identity and broader casting/model quality |
| Fairness/completion | 314 scenarios across 13 missions and 50 objectives; all supported dispositions covered | Pending | Consequence-bearing early/middle/late journeys, non-success routes, fair discovery |
| Long campaigns | Rich 15/500-pair persistence, branch, projection and retrieval measurements; reviewed numeric-label fix and installed browser lookup probe | Natural long play pending | Running 5,000-pair workload; model-selected retrieval, unanchored recall, native-wire growth and turn latency |
| Recovery/coexistence | Provider finish metadata, bounded retry feedback, gesture ownership, swipe Retry, and dialog cancellation regressions pass | Actual Stop and Retry Close abort transport without commits; real Retry recovery; mobile correction geometry/cancel | Sustained recovery, other mobile controls, provider changes, ordinary chats and coexistence |

## Findings

Two production defects were reproduced in the first live attempt:

1. Native response extraction discarded `finish_reason: length`, so an empty response that exhausted 8192 completion tokens was reported as reasoning-only instead of token-limited. An initial raw-response fix passed the existing gate but independent review found Claude schema, llama.cpp array, and text/instruct compatibility regressions. Revision is required before publication or further semantic scoring.
2. Continuity and direction retries did not receive their strict validator's diagnostics. The existing coordinator already supported role-local feedback; the focused analyst adapters dropped it. The candidate retains at most eight diagnostics of 240 characters each and supplies them only as retry feedback. Source evidence, validators, schemas, and retry counts remain authoritative. Independent review found no blockers in this change.

These are observed recovery failures, not evidence that failed model output should be accepted. The revised provider adapter passed independent review after retaining native schema extraction, array response metadata, instruct/preset cleanup, and provider-source snapshots. The scoped fixes and integration regression are included with this report; broader stabilization remains in progress.

## First live batch: baseline observations

- Fresh Ashes campaign with synthetic player Avery Quinn. Opening completed through two real provider requests.
- First natural player turn asked to enter the ready room and discuss command authority. Eight subsequent provider calls ran across parallel analysis and recovery; the turn paused after approximately 305 seconds without a new assistant reply or accepted pair.
- Interpreter evidence initially used an ellipsis rather than a continuous quote; its existing feedback retry succeeded. Direction initially selected an invalid target; a later attempt succeeded. Continuity alternated between token exhaustion and invalid source quotes and did not settle.
- Total real requests: 10 including opening. This is not a successful sustained soak; accepted post-opening pairs: zero.
- Native Mission controls successfully marked command handover complete and then still underway. Page reload followed by explicit campaign reopen retained the player decision, matching the durable save, with confirmation required and state revision 3. This is live UI/persistence evidence, not natural semantic completion evidence. The initial immediate reload probe raced host startup; the explicit reopening probe passed.
- A Stop probe ran after the turn was already terminal. Stop remains unverified.
- At 390px and 320px widths, the installed Mission correction dialog and each action remained reachable inside the viewport; Cancel preserved exact campaign state. Screenshots were inspected. This is resized Chromium geometry/cancellation evidence, not physical mobile-device coverage.

## Reviewed candidate: installed-host checks

- Candidate production patch SHA256 `e9757c7d9ec5c47efa7f72ae7df99c024995e4f0ba202a3dd43f2f9d01ab0299`; all 666 installed files matched the working-tree source at installation.
- Replayed one naturally captured HTTP envelope through the actual installed native request path: `provider_token_limit`, `finishReason: length`, `maxTokens: 8192`, one request, no campaign-state mutation. The output-limit Settings guidance appeared. This replay made no real provider call.
- Actual browser-loaded native SillyTavern extractors retained Claude `tool_use.input` and native-schema certification in a synthetic-transport check. This verifies installed adapter/native-function compatibility, not a live Claude-provider response.
- Native Stop canceled three real concurrent analysis transports (`net::ERR_ABORTED`), left memory/durable state and chat count unchanged, and started no retries during the five-second observation. Controlled delayed-result regression separately covers callbacks beyond that window.
- An earlier Stop harness used blank Send, which expands to the copied host's `Continue.` setting. Stop canceled its three transports and preserved campaign state, but the harness's unchanged-chat assertion correctly failed. The exact added test message was removed through native deletion; the verified rerun used native regeneration of the pending player message.
- First batch ended with 16 real provider requests (10 baseline plus six canceled Stop-probe requests) and one HTTP replay. The three Claude extractor fixture calls used synthetic transport and are excluded.
- A second bounded gameplay batch uses the existing copied GLM Flash profile for the dedicated account's reasoning lane, following repeated exhaustion by the original GLM5.2 thinking configuration. Native narration remains on its original profile. Quality and latency must be attributed to this separate configuration.
- First attempt in that configuration: four real calls; interpreter and continuity returned valid proposals, while direction twice selected an objective ID as a continuity-thread target. The retry received the validator code, confirming feedback transport, but the code alone did not produce recovery. Narration paused honestly, with chat still two messages and authoritative mission revision unchanged. This attempt is not a semantic gameplay pass.
- Focused direction rejection now retains its stable error code and adds a bounded diagnostic identifying the invalid move/target and the permitted source-bound target set. It never presents shortened identifiers as legal IDs and cannot invoke coercion on malformed object values. Independent review and regressions cover both cases.
- Final candidate production patch SHA256 `7ee20f541169848c8c1b731237465c2f961ef27e40907a12162ae172c77eb527`; all 666 installed files matched source. The final full gate passed all 226 checks; log: `final-candidate-gate.log`.
- Live revalidation of the same pending player turn succeeded through five real calls (three initial analysts, one direction retry, one narration). Direction first returned `respond-to-player` with an objective target, then corrected it after receiving the actionable diagnostic. Narration answered the command-boundary question. The handover remained incomplete with player confirmation required. This is one successful recovery, not a sustained-soak result.
- A following natural turn accepts that reply and asks about emergency authority before formalizing the handover. Narration completed, again leaving handover incomplete. It required eight real calls, including continuity retries across two preparation passes; that operation boundary is under investigation before claiming turn-wide retry boundedness.
- Current durable snapshot matches in-memory authority at custody revision 5, with exactly two accepted-pair receipts and two director receipts. The latest assistant draft is not yet accepted. The two completed narrations do not satisfy the predeclared 20-pair, three-timeline minimum.

## Evidence and remaining work

The tracked `tools/scripts/test-objective-progress-app-recovery.mjs` promotes the combined correction journey into the alpha gate. Independent review and its focused run pass. It controls provider responses and one storage-write failure while exercising production app, settlement, correction, replay, and hydration; it does not patch campaign state directly. Its retry is a failed-correction retry, not a native generation Retry.

Ignored `artifacts/stabilization-20260914/` holds baseline gate output, installed-file manifests, provider request/response records, request timestamps, controlled journey probe/output, correction snapshots, and screenshots. Raw provider records contain synthetic story material; copied provider configuration and credentials remain outside tracked evidence. The baseline correction snapshot is preserved separately from candidate runs.

No full-soak, aggregate semantic-quality, latency-percentile, ordinary-chat, or all-mission completion claim is supported yet. Continue the bounded objective journey, then selected-swipe branching and private-information continuation. All unexercised scenarios in the plan remain required by the active goal.

## Native generation ownership follow-up

The first recovery slice was published as `a89d2ff149757917f9714a1b2a4863f01430ff18`; all 666 installed production files were verified against that commit. Two real accepted pairs remain the semantic-soak total.

A controlled native-host probe then reproduced a second defect: one Send gesture ran four continuity attempts, because the MESSAGE_SENT observer failed and the waiting interceptor granted another recovery pass. An explicit Retry added two more attempts. Both delayed responses and fresh-page immediate responses reproduced the four-attempt initial pass. These injected HTTP responses are not real-provider semantic evidence.

The reviewed follow-up correlates native GENERATION_STARTED with observer/interceptor work, checks ownership again inside the serialized retry operation, and preserves explicit Retry and cancellation. Focused regression and independent review cover simultaneous interceptors, fast/in-flight failures, Stop followed by End and automatic generation, and successful Retry narration handoff. Installed candidate production patch SHA256: `5853e5a34eea865547e05b78523e68b0698b14579da444af4a340fdfd3a8efd0`; all 666 files match source.

Installed-host verification of this follow-up is temporarily prevented by a separately reproduced native-branch failure. Actual Create branch UI supplies valid lineage, but the timeline transaction rejects with `DIRECTIVE_BRANCH_TIME_REBUILD_FAILED`. Its pending recovery reopens the incomplete child on subsequent chat changes. The parent durable save remains unchanged at revision 5 and can be opened directly, but a new Generate is blocked by that pending transaction. A resulting zero-call retry probe is recorded as inconclusive, not passing. Root-cause investigation is in progress; no save or operation records have been manually repaired.

The reviewed retry candidate passed the complete 227-check gate (`gesture-final-gate.log`, exit 0). Its installed-host replay remains inconclusive until branch recovery is repaired. Read-only diagnosis confirms an empty-invalidation bug: a tail branch retains all five messages, but branch reconstruction unconditionally asks the time invalidator to remove an empty set. The invalidator correctly rejects that input; branch reconstruction must skip the unnecessary operation. A focused regression and scoped correction are in progress.

## Verified native retry and tail-branch repairs

The tail-branch repair now skips time invalidation only when no messages were discarded. Regression coverage preserves the entire ledger and custody revision for a retained tail, and still removes discarded time entries, decisions, and accepted-pair effects for a truncated branch. Independent review passed both authoritative-time and native-branch runtime suites.

Installed candidate patch SHA256 `bc0d3281ae4e39eec208f328396a0e9f6487e1ad0c3d76eefdb8fd5025301fb9` matches all 666 production files. Reload resumed the existing journal through normal production recovery and completed it. The previous active save was retired as designed after preservation in checkpoint `checkpoint.1789416039211.2`; that checkpoint equals the pre-branch parent state except for its validated transcript attestation. Child `save.1789416039211.1` retains five messages and the same 50 seconds of accepted time, with hydrated state exactly matching durable storage at custody revision 5. No operation or save record was manually repaired.

Installed native fault checks now pass with delayed and immediate HTTP responses: two initial continuity attempts, then exactly two more after clicking Retry, six intercepted HTTP requests total per scenario, with unchanged campaign state and cleanup through native message deletion. Evidence: `native-gesture-candidate-recovered-delayed.json` and `native-gesture-candidate-reopened-fast.json`. This proves real host lifecycle accounting with controlled responses, not real-provider semantic quality or successful narration handoff.

An intervening fresh-page harness attempted verification before reopening the campaign; the host was on its default Assistant chat. Its assertions failed and one unexpected generation request was intercepted and aborted. The harness now requires a nonnull campaign state and an exact native chat/binding match before sending. Explicit production campaign reopening restored the child, after which the immediate-response check passed. This failed setup is excluded from passing scenarios.

The final combined release gate passed all 227 checks (`gesture-branch-final-gate.log`, exit 0). Total real provider requests remain 33 and unique accepted pairs remain two; copying their history into a branch does not create additional semantic samples. The recovered child has not yet been continued through real-provider play. All remaining areas and the 20-pair/three-timeline minimum remain open.

## Third live batch and selected-swipe findings

On published `2cc02e8ae188e202e30cb0d49ca822c08423db8b`, the recovered branch completed one additional real pair after two explicit Retry gestures. The interpreter first returned a forbidden extra field, then incomplete JSON, then an evidence-quote mismatch, then incomplete JSON again. Read-only inspection confirmed both malformed responses were missing only the final root brace despite `finish_reason: stop`; the emitted examples and payloads were valid and the shared parser ran normally. Validators remained strict. The final Retry produced a valid interpretation and successfully handed off to native narration. All six earlier failed analysis calls left authority unchanged; the successful pass committed exactly one pair. Current unique-pair total: three, with real continuation on two timelines.

The acknowledgement turn preserves the player's confirmation-required handover decision. Narration acknowledges the handover and answers the request for an immediate operational concern; it does not move the player into a test or complete readiness work. Eight calls were used across the initial gesture and two explicit retries, including one successful narration. Individual measured request durations ranged from 9.7 to 53.1 seconds; manual inspection pauses are excluded from these request durations and no population latency claim is made.

A native alternative-reply attempt exposed two further defects:

- The direction prompt/schema accepted global constraint IDs in `requires`, but the compiler only accepts conditions attached to the selected target. A continuity target has no such conditions. The model's apparently valid direction therefore failed during deterministic reconciliation without an actionable explanation. The candidate aligns the target-specific contract and feedback while retaining the compiler's final guard.
- After failed swipe preparation, dialog Retry called native `Generate('swipe')` directly. The host had reset the selected slot to zero after the initial failure; Retry overwrote the original draft instead of reserving a new alternative. The preserved pre-swipe-save-evidence.json and swipe-retry-after.json show one swipe of 2470 characters becoming one swipe of 2196 characters, with the original absent. The candidate routes Retry through the native swipe lifecycle. This is a reproduced draft-loss defect; the accepted-pair count stayed at three.

Natural draft/state before controlled revalidation was preserved through production Save Game in checkpoint `checkpoint.1789418104806.2`. Its creation left active state unchanged. The original overwritten draft remains in diagnostic evidence; no manual transcript repair has been treated as gameplay proof.

The disposable account's Utility and Reasoning lanes were Auto/not-run, so their previous complex requests used Prompt JSON. Production Test Provider passed connectivity and native-schema certification for both exact configured GLM profiles, using four real requests. Auto now selects native schema. This is a new configuration boundary and capability proof, not evidence that complex semantic output will be correct. Raw request metrics and all classification boundaries are preserved in ignored artifacts.

## Authored consequence coverage expansion

The source inventory verified all 13 Ashes definition hashes and all 50 objective IDs against the support matrix. Fourteen authored scenarios and additional exact award assertions close the ten disposition-coverage gaps. The campaign harness passes 13 mission contracts and 314 scenarios: 174 terminal/transition cases and 140 active or negative cases. All supported objective dispositions now have explicit authored-scenario assertions; 48 scenarios assert exact award sets. Chapter 1 and 2 optional declines preserve closure and transition, and all three Open Orders III awards have direct completion/cost/decline/failure boundaries.

Definitions and the harness were not changed. No unreachable gate or production consequence defect was proven. Seven required objectives remain authored as success-only duties; Rhee apprehension is optional without a decline disposition but can remain unresolved without blocking closure. These source constraints still need live journey adjudication. Controlled scenarios do not prove model interpretation, player discovery, fair narration, or full-campaign completion. The detailed coverage map is retained in `ashes-fairness-coverage.md`.

## Verified direction and swipe repair candidate

The reviewed candidate matches all 666 installed production files (production patch SHA256 `03df2c795e71dd7dcca3670375a72b92cc94e960319cc7387f797c5a37eb332c`). Independent reviews approved the target-specific direction contract and the host-owned swipe continuation. Source guards, Stop/import ordering, unsupported host APIs, empty/whitespace output, and repeated failure are covered by focused regressions. The native API can preserve a nonempty partial stream without exposing a separate failure result; that native partial-output distinction remains a documented limitation.

A controlled installed-host test forced two invalid direction responses, then a valid Retry response and streamed narration. It made four intercepted HTTP calls with no real provider request. The original swipe remained unchanged, the new response occupied slot 1, and accepted pairs, mission, and time were unchanged. Evidence: `native-swipe-retry-candidate.json`. Production Load Game then restored the natural checkpoint into `save.1789418642122.1`; draft text, time, corrections, three accepted pairs, and durable state were verified (`restored-natural-checkpoint.json`).

With actual certified native-schema analysis, a real alternative generation completed in two provider calls. Its raw direction request contains the expected native JSON schema. Both alternatives were retained; selecting swipe 0 and then swipe 1 left accepted pairs at three (`selected-real-swipe.json`). The next real player message is testing acceptance of swipe 1. At this checkpoint that acceptance is still pending: duration/pacing interpretation rejected while the other analyst is still running. This is not yet a completed selected-swipe acceptance/reload journey.

The complete final gate passed 227 checks, including the 314 authored campaign scenarios (`direction-swipe-final-gate.log`, exit 0). The repair slice was published to main as `ef20acd0cc235406beb357e1b7fdd7f13daa2820`, verified through GitHub CLI, and all 666 installed production files matched that commit. The active goal retains the new interpreter finding and all remaining live scenarios.

## Native-schema portability and selected-swipe acceptance

The real selected-swipe acceptance attempt (requests 93–97) ended with Retry available and no new accepted pair. Both interpreter responses were valid JSON with a normal stop finish, but supplied `durationSeconds: 0`, an assistant source slot, and an empty duration quote. The transmitted schema makes those fields optional and non-null, with positive duration and quote-length constraints. The outputs violated that schema; Directive correctly rejected them. The retry received the exact diagnostics and repeated the invalid placeholders. Pacing independently cited an unauthored requirement, then an earlier player source and an altered assistant quote.

Earlier parseable Prompt JSON responses omitted these optional duration fields. The first two native-schema interpreter responses supplied invalid placeholders. This supports a provider/schema portability problem, but does not establish its internal cause. The existing simple boolean certification probe did not test optional constrained fields. Certification is capability evidence only, not complex-turn correctness.

At 20:54 UTC only the disposable account's Utility lane was explicitly changed to Prompt JSON; Reasoning retained native-schema certification. One actual dialog Retry then supplied no invalid duration placeholders, but its interpreter attempts failed an exact-source quote and JSON parsing respectively. This comparison has not recovered the gameplay turn. Semantic validators remain unchanged, both real swipe alternatives remain available, and the unique accepted-pair count remains three. Raw records and configuration boundaries are retained locally; provider credentials are excluded from tracked evidence.

That comparison ended after continuity reached its 300-second request limit and its retry failed grounding validation (requests 98–102). No accepted pair or narration was added. The next certification revision now asks for exact `{ "ok": true }` while declaring the constrained optional duration fields and requiring their omission. Providers that materialize those fields do not earn native certification. The v2 fingerprint invalidates earlier weak certifications; Auto uses Prompt JSON until recertified, and explicit Native Schema remains unavailable until certification succeeds. Independent review and all 227 gate checks passed (`certification-final-gate.log`). The stricter probe covers the reproduced omission failure, not universal schema compliance. Installed-host revalidation is still required for this revision.

While waiting on the real Retry, the native Stop control was hidden and the dialog remained busy. Source tracing confirmed that Close/Escape/backdrop abort the dialog controller without passing its signal into analysis. This prevents the later narration handoff but can leave analysis and settlement running. A scoped cancellation repair is in progress; no passing live Close test is claimed for the current build.

## Installed certification and Retry cancellation verification

Certification revision `68145b67b317acdad5df7c65deed08949f78b11f` was published and verified on GitHub. All 666 installed production files matched its Git content, allowing Windows checkout line endings. A production Test Provider call reproduced the exact unwanted duration placeholders in the stronger native probe. It correctly certified this configuration for Prompt JSON only (`live-certification-reasoning.json`, requests 103–104). Utility remains explicitly Prompt JSON; Reasoning Auto now uses its v2 Prompt JSON result. Reload and explicit campaign reopen preserved full state, the pending player message, and both selected-swipe alternatives.

The cancellation candidate passes the dialog attempt signal through the native bridge into queued turn preparation and its analysis controller. Dismissal cancels only that attempt. Existing mission settlement guards prevent a canceled analysis result from committing. Close, Escape, and backdrop regressions exercise abort-ignoring delayed providers, unchanged durable state, queue release, and a fresh gesture while the old response is still outstanding. The new gesture remains independent and commits once. Independent review approved the change and confirmed successful narration handoff and existing global Stop semantics remain covered.

Installed native preparation used four synthetic responses to create the blocked dialog without changing campaign state (requests 105–108). Actual dialog Retry then issued one real interpreter request; other successful proposals were reused. Clicking Close aborted that request at the transport boundary, left memory, durable state, and all chat messages unchanged, and started no subsequent request during the observation. The persistent recorder captured `net::ERR_ABORTED` within 100 ms of the Close action (`close-live-retry-verified.json`). The short-lived Close probe attached after the request began and missed its request-failed event; reconciliation against the recorder established the result without another provider call. This proves installed Close cancellation, not successful gameplay recovery. Unique accepted pairs remain three.

The complete final cancellation gate passed all 227 checks (`dialog-cancellation-final-gate.log`, exit 0). A subsequent exact reload preserved campaign and transcript state and discarded the controlled attempt's in-memory proposal cache. A fresh native Generate then resumed the real pending player turn with real calls in all three analysis lanes. Its result remains outside the completed cancellation claim.

## Current-source evidence contracts and recovered swipe acceptance

The next source review reproduced two prompt ambiguities and one schema mismatch. The interpreter's generic time example allowed implicit action without showing its required source and quote fields; a real response omitted them. Its retry manufactured a closing quotation mark and copied historical participation as current evidence. The revised examples are coherent, include complete implicit-action evidence, and explicitly distinguish current source-pair quotations from historical context. Existing time, pacing, and mission validators are unchanged.

Continuity exposed arbitrary `authoredRef` strings even though validation accepts a closed authored-ID set. The schema now uses that request-specific set plus null, including a valid null-only case. Rejected quote diagnostics retain their stable code and add bounded change index, source slot, and length/non-contiguous/type detail. Replaying captured failures identified an exact 260-character excerpt over the 240-character limit, then two excerpts with invented boundary punctuation. Independent review approved both repairs; all 227 gate checks passed (`evidence-contract-final-gate.log`).

A separate five-call sampler-policy probe reached output limits in both interpreter and continuity, then was stopped through native Stop without state changes. Switching to Directive samplers lowered temperature but also removed the profile's reasoning-effort override, so this is not a temperature-only comparison. The original profile policy was restored before testing the repairs.

Installed production patch `353b2f8ed0ae4bd7c19b3ee77c0708339c2e7b872ac48a2db3b9ff3ff5c0ce4e` matched all 666 source files. On the same real pending turn, interpretation passed first attempt with a valid current-player quote, no invented quote punctuation, and no historical pacing participation. Continuity required its bounded retry and one explicit dialog Retry; six real calls including narration recovered the turn (requests 120–125). The narrator sent the requested notice, withheld test authorization, and moved to engineering only after fulfilling the player's stated condition.

The durable accepted receipt names assistant message 6, swipe 1, text hash `f30fd830`, and current player message 7. Both real alternatives remain unchanged. Memory and storage match at custody revision 9; time is 90 seconds and the handover correction still requires player confirmation. `selected-swipe-acceptance-verified.json` proves this exact source identity. Unique accepted pairs now total four. This single recovered journey does not establish sustained reliability or complete branch, information, fairness, or scale coverage.

## Selected-tail branch and private-briefing findings

After the selected swipe was accepted, native Create branch at the retained assistant tail produced child save.1789421119789.1 and preserved the parent as checkpoint.1789421119789.2. Exact accepted source pairs, both swipe alternatives, and the time ledger survived. The mission run ID changed as required for the child; a first harness comparison incorrectly treated that valid remapping as a defect. Offline reconciliation of the captured before/after evidence passed without repeating the branch action. Reload and production reopen then preserved the entire child state and transcript (`selected-tail-branch-verified.json`). This covers a retained tail, not truncation.

Two following real turns completed without retries (requests 126–134), bringing the unique accepted-pair count to six. Cross moved into her office, closed the door, and agreed that “amber notebook” meant risks reviewed without test authorization. The subsequent accepted record binds this agreement to Cross alone. The narrator's following diagnostic officer did not use the private phrase and distinguished a report from seeing the captain's notice. However, two defects prevent passing the journey:

- The global supporting-character guide exposed future mission character Mira Solenn. The narrator invented a lieutenant rank and ship diagnostics billet for her in the Prelude. Neither current mission references nor People supplied Mira. This draft remains unaccepted; no authoritative introduction is being treated as valid.
- The preceding player's compound conditional request already generated a `heard` receipt for Cross from “Once we are alone, I say quietly…”, although her cooperation and the private setting were only depicted in the following assistant reply. Existing deterministic validators prove quote custody and recipient shape, not enacted delivery. The player commitment may survive, but its unmet condition must not grant audience access.

Batch 6 closed with 15 real requests and three additional accepted pairs, cumulative 90 real requests and six unique pairs. All broader completion thresholds remain unmet. Captured request/response data, exact state/storage comparisons, and `private-briefing-observations.json` preserve the failures for repair and revalidation.

The three completed turns in batch 6 took 161.3, 32.2, and 26.4 seconds from first request start to final response receipt, respectively (six, five, and four requests). The first includes a manual Retry inspection interval; the last produced the invalid unaccepted cast. These are three individual observations under the restored profile configuration, not typical or tail population latency (`batch6-turn-metrics.json`).

## Character guidance and conditional-delivery repair candidate

The first, unpublished candidate gave supporting-character performance guides authored `eligibleMissionIds`. That mission-wide design was superseded after the live Daro casting failure below. The final candidate uses `guideEligibility` entries with a mission ID and an existing authored mission predicate. All 20 Ashes support records were audited; dataset version 1.3.0 adds typed service information for Crewman Daro Tem. The optional schema property preserves legacy/custom document loading; missing or invalid eligibility omits supporting guides. Bundled Ashes tests require complete eligibility metadata.

This bounds guide availability, not character presence, location, billet, or knowledge. Current People introductions use emergent IDs and cannot safely bind an authored supporting identity by name; the exact-ID fallback does not claim to solve that separate limitation.

The continuity policy distinguishes direct enacted player speech from speech conditional on an unmet NPC response or environmental precondition. Supported intent or commitment may be retained while audience metadata is omitted. Later accepted delivery can establish access. The extraction corpus now tests this negative alongside confirmed delivery and ordinary direct speech, while continuing to label source-valid semantic mistakes as mistakes rather than structural failures.

The failed timeline will remain preserved. Native message invalidation would remove the premature receipt and dependency-prune the later valid receipt; historical rebuilding does not make fresh model calls to reconstruct both. No one-off state rewrite is part of this validation. A fresh continuation from the pre-briefing checkpoint will test corrected extraction and subsequent narration through production gameplay.

The installed candidate matched all 666 files (patch `8e3442e01f692e2317b6119990c3535653095d2c4aee8ee9863311277afc9898`). Native alternative generation used two real requests, 135–136. Its wire payload excludes Mira and the rejected draft, includes only Chen/Rhee/Tem guides, and is 79,258 characters versus the prior 93,023. Both alternatives survive. Accepted-pair receipts, mission, world, and time are unchanged; a fresh direction receipt and custody revision are expected native swipe preparation effects.

The replacement nevertheless casts Lieutenant Daro Tem. Authored source identifies him as Crewman Daro Tem in propulsion maintenance, and his conditional storyline was not yet discovered. This reproduces a remaining casting/disclosure failure: mission-wide eligibility is insufficient for conditional supporting characters. The draft remains unaccepted; this is not a passing narrator outcome. Precise activation through existing mission authority is under investigation.

Production Save Game preserved the failure in `checkpoint.1789422914632.2`, then Load Game created a fresh continuation from the untouched four-pair pre-briefing checkpoint. The original conditional player text was submitted unchanged. Five real analysis requests, 137–141, paused before narration and left four accepted pairs/time90 intact. This attempt is not a semantic pass or an additional accepted sample. The next retry remains bounded by the declared batch.

The first full candidate gate failed the existing desktop orbit neutral assertion; its isolated rerun passed unchanged. The next full run reached a People keyboard-reorder timeout. These failures remain recorded and prevent a release-gate success claim. The orbit test's fixed wait barely exceeds the transition plus frame-start delay. A bounded state wait and diagnostic focus synchronization are being reviewed; no production UI defect is yet established by those two failures.

The conditional replay recovered through one actual Retry: the episode role returned a valid continuation, cached successful analysts were reused, and narration enacted the private conversation (142–143). The earlier block came from two episode proposals using a significance token as a boundary reason. The second request carried no validator feedback; the existing episode enum was already correct. A bounded adapter/evaluator feedback repair is in progress.

Accepted replay state retains the player's pending proposal and no audience grant. The next natural follow-up completed through four first-attempt requests (144–147), accepting the enacted assistant exchange. Its single amber-code receipt identifies only Cross, with main and audience sources bound to assistant message 10, hash `2e557dc4`; the earlier proposal remains unchanged without access. Memory and durable storage match. Narration honors the player's one-meaning clarification and leaves test authorization withheld. `conditional-delivery-replay-verified.json` captures the negative and positive assertions. This is one source-matched replay and a subsequent natural positive, not general model accuracy.

Batch 7 closed after 13 real requests, with one new distinct accepted pair and one repeated-source acceptance. Cumulative totals are 103 real requests, seven distinct accepted pairs, and one explicitly separate replay sample. The current continuation has six local receipts. Casting remains open because the stage-only candidate exposed undiscovered Daro; the next candidate will use existing mission predicates and typed authored service information.

## Predicate-bound guides and completed private-delivery slice

Final guide eligibility uses the existing mission predicate validator/evaluator against current mission authority. An eligible mission must have exactly one valid, satisfied rule. Accepted exact person/participant IDs retain their own guide; names and prose never create identity. Rhee's Prelude guide requires confirmed notice distribution. Daro's Prelude guide follows the authored care storyline predicate. Their conditional epilogue guides require the corresponding aftermath/capability facts. Ordinary regional epilogue participants remain available before settlement accounting. Invalid, missing, duplicate, or unsatisfied rules fail closed.

After installation and exact reload, three natural turns continued the fresh timeline (requests 148–162). The first introduced local Lieutenant Corwin Halvard. Its actual narration request contains only Lysa Chen's supporting guide, excludes Mira/Rhee/Daro throughout the request, and projects only Cross in character information. The next turn accepted Halvard's introduction and asked what instructions he had received. His reply did not use the private code. His statement that Cross relayed the notice is a current secondhand claim, not proof of an earlier historical communication.

Before explicit delivery, Halvard has no audience receipt. Player message 17 directly explains the code to him in Cross's presence. The resulting heard receipt names only Cross and Halvard, and binds both main and audience evidence to that exact message, hash `5795e34f`. His following readback preserves risks reviewed, test not authorized, and the review-team restriction. The readback remains an unaccepted draft at this checkpoint. This proves the selected negative/positive journey, not universal confidentiality or casting accuracy.

The episode repair carries at most eight 240-character validator diagnostics through the existing runtime callback into a wire-only retry field. Request identity, semantic envelope, budgets, validators, and cancellation remain unchanged. One controlled invalid episode response at recorded request 158 reproduced the two errors `seal boundaryReason is unsupported` and `seal foregroundQuestion must be null`. Real automatic retry 161 received both exact errors, returned a valid continuation, and the turn committed once. The injected summary did not enter state. Other analysts and narration in the turn used real providers. Recorder reconciliation compares the exact injected response, identical semantic envelope, and retry feedback; recorder allowlisting prevents comparing its body hash with the browser's full-body hash. No extra provider calls were needed to reconcile the evidence.

Batch 8 closed with 14 real requests, one synthetic rejected response, and three additional distinct accepted pairs. Cumulative totals are 117 real requests, ten distinct accepted pairs, and one repeated-source sample. Current continuation: nine local accepted receipts, custody revision 16, nineteen chat messages. Evidence is retained in `predicate-guided-narrator-wire.json`, `conditional-delivery-replay-verified.json`, and `private-delivery-and-episode-live-verified.json` under the ignored soak artifacts.

The UI gate fixtures now wait for the actual neutral orbit state and focused current People handle, retaining the same geometry and per-key movement assertions. They do not retry keys or change production UI behavior. Independent review approved the fixes. Final combined gate passed all 227 checks (`character-boundaries-final-gate.log`), including 314 authored scenarios across 13 missions. The earlier gate failures remain recorded; no production UI cause is claimed for the keyboard timeout.

One separate background failure occurred at request 156 after Halvard's introduction. The provider returned normal stop in 21.5 seconds, but produced a generic records object instead of the required dossier batch. Prompt JSON was told to match a supplied schema that the textual request omitted. Core gameplay and the accepted introduction survived; the optional public dossier remained empty with a failed job. A schema-visibility repair has focused regression coverage and independent review, but its installed retry is still pending and is excluded from this completed slice.

## People dossier schema and passive-reload recovery

The optional dossier author now includes its exact finalized schema in the textual system prompt used by Prompt JSON. This retains native schema metadata, per-person identity constraints, configured field limits, validator strictness, retry budget, and cancellation. A regression inspects the generation-router request and compares the serialized schema to its declared schema. Independent review and the first full 227-check gate passed (`dossier-final-gate.log`).

Installed revalidation exposed a second boundary. An explicit production `retryPendingPeopleDossiers()` returned `queued: 1` and durably changed the failed job to pending, but no provider request followed for over twelve minutes. Chat, mission, time, and accepted sources were unchanged. Passive chat reconciliation called the same dossier-pause function as narration and never released its narration-active flag. The previous reload test masked the problem by emitting another Generation End before checking that the author started.

Chat reconciliation now owns a pause token and releases only that token when its work settles. A newer native generation supersedes it. Deferred internal chat opens retain their pause until deferred reconciliation runs; Stop and active-analysis guards remain authoritative. Focused regressions require the reloaded retry to start before any subsequent gameplay generation and prevent an older chat completion from releasing a newer native generation. The existing Generation End behavior retains its ordered host-event assumption; this slice does not claim support for arbitrary reordered host terminal events.

The combined candidate patch `6909a9e3e82fcb7767cb93fb9fcee0420812ede2f5b545090aade5d0aa92dbc2` matched all 666 installed production files. Reopening the existing disposable save resumed the already queued job through one real provider request, 163. Its textual schema was present; the private amber code was absent from the dossier request. The provider returned a valid dossier in 4,807 ms with a normal stop finish. Public fields merged into the same source-bound introduction and the pending job disappeared. Chat, accepted-pair receipts, mission, world, time, and command bearing stayed unchanged; memory and storage match at custody revision 19. Generated biography is public enrichment, not independent confirmation of previously narrated historical events.

Evidence: `dossier-queued-without-request-verified.json`, `queued-dossier-reload-result.json`, and `dossier-recovery-live-verified.json`. Installed People and Mission visible-text checks separately preserved Halvard's introduction and omitted the checked undiscovered supporting names; the pre-recovery People detail showed the known introduction with the optional biography absent. This covers selected disclosure/UI assertions, not every hidden field or notification.

Independent lifecycle review approved the repair. The combined full gate passed all 227 checks (`dossier-lifecycle-final-gate.log`, exit 0). No additional accepted pair is credited for the background retry. Rich synthetic-state performance work and remaining natural campaign/information journeys continue separately.

## Rich controlled-state persistence, retrieval, and branch measurements

Three standalone scripts provide a source-linked rich fixture, a measured runner, and an independent expected-state oracle. They exercise actual accepted-pair preparation, continuity materialization, contribution/episode/People settlement, segmented persistence, projection, prompt construction, retrieval, and branch reconstruction. They make no provider requests and do not import fabricated state into the live host. Every accepted pair is persisted through consecutive production revisions.

The completed run used Node 24.16.0, one warm-up and five fixed-workload repetitions. Final evidence is `rich-campaign-offline.json`. Earlier packet-size results were withdrawn after a harness check found that the projection wrapper had been passed instead of its `.projection`; the final runner asserts successful projection, exact sealed-story/People counts, and nonempty accepted-story content before scoring bounds. The source oracle also rejects a count-preserving duplicate assistant source, rather than trusting only counts and valid hashes.

| Workload | Small | Medium |
| --- | ---: | ---: |
| Accepted pairs / transcript rows | 15 / 31 | 500 / 1,001 |
| Threads / sealed episodes / people | 5 / 2 / 4 | 100 / 50 / 20 |
| Final state bytes | 46,734 | 1,000,083 |
| Storage segments | 1 | 8 |
| Runtime prompt packet characters | 58,739 | 63,128 |
| Exact-ID lookup recall | 4/4 | 4/4 |
| Lexical title lookup recall | 4/4 | 0/4 |
| Paraphrased query recall | 0/4 | 0/4 |

Both workloads passed exact storage round trips, correction/status/relationship retention, source and receipt custody, parent immutability, and four branch cuts. Medium median load was 266.6 ms, player projection 11.3 ms, prompt construction 4.11 ms, and pooled direct retrieval 5.56 ms. Maximum branch reconstruction among the exercised cuts was 3.80 seconds. These are synthetic in-memory-adapter measurements, not disk/network or browser latency, and the small sample does not establish population tail percentiles or a same-workload regression ratio.

The original crowded numeric-label retrieval misses remain recorded failures. Tokenization dropped short numeric discriminators; an independently reviewed fix is described below. Paraphrase results must not be represented as semantic retrieval capability. Omitted obligations remain stored, and none of these lookups mutates authoritative state.

The defined 5,000-pair workload with five repetitions started at 23:04 UTC on September 14 and is still running; no completed result is credited. Source identity, patch, and live progress/status are retained under `long-rich-soak-20260914T230305Z/`. Runtime packet bounds also do not establish a full native-wire bound: actual narration request 169 contains 88,351 characters once the host combines transcript, preset, and Directive context. Natural long-play recall, model-selected lookup expansion, physical browser responsiveness, and long-workload performance remain open.

## Partial documents, private revisions, and bounded narration timing

The player wrote a two-line private note, showing Halvard only the observation-only diagnostic instruction while concealing a violet-code definition. The initial turn failed continuity validation: an `open` lacked its required same-response fact, and a later attempt used `narrated-fact` for current-player evidence. One explicit Retry recovered. Accepted state records the concealed line as a player commitment without an audience; only the visible line has Halvard access. His readback omits the hidden line. The next turn accepts that readback and preserves the disclosure boundary. Evidence: `partial-document-disclosure-verified.json`.

The focused continuity prompt now states both already-enforced constraints: every local open needs a same-response addFact except a standalone current-player obligation; current-player facts must use player-commitment or an information-bearing character-claim, while previous-assistant facts cannot use player-commitment. Validators and acceptance remain unchanged. Focused regressions retain the observed invalid shapes and inspect the outgoing prompt. Real request 178 contains both rules; its turn completed with four first-attempt provider calls.

That continued narration separately claimed an event happened twenty minutes earlier although the entire accepted campaign clock was 245 seconds. The actual request already supplied the clock and authority policy. The draft remained unaccepted. The existing time interpreter rejects retrospective `ago` language as enacted forward duration, so the prose error is not evidence of an added twenty minutes in the ledger. Narration guidance now prohibits intervals larger than elapsed campaign time for events occurring at or after opening, and calls for qualitative callbacks when no prior timestamp supports an exact interval. Backstory is not constrained to campaign duration.

One native alternative generation preserved the rejected swipe and all accepted mission/world/time/source state. Its selected replacement uses a qualitative callback. Actual request 175 contains the final policy and excludes the rejected draft; subsequent acceptance binds message 22, swipe 1, hash `ad30c80f`. This is one live improvement, not a deterministic guarantee about arbitrary generated prose. Evidence: `temporal-alternative-live-verified.json` and `cross-revision-live-verified.json`.

In the following closed-office exchange, the player revised the amber code to defer a decision until a written mitigation report, still withholding test authorization. Only Cross receives the revised information; Halvard retains the older receipt and has not received the revision. Cross's readback explicitly promises not to relay it. Memory and storage match at custody 23 with twelve local accepted receipts.

The next Halvard recall turn initially paused after continuity exhausted its output limit twice (requests 180–184). Accepted state remained exactly unchanged and the UI correctly reported the limit and that narration had not begun. One explicit Retry used two real requests, 185–186, and reused the successful analysts. Halvard repeated the old definition without the private written-report revision. A following direct player update completed with four first-attempt requests, 187–190. It adds one heard grant for Halvard bound to player message 27, hash `a50d91da`; the earlier private receipts remain unchanged. His revised readback retains the no-test-authorization and observation-only restrictions. The hidden violet line remains without access. Evidence: `old-code-readback-live-verified.json` and `code-update-live-verified.json`.

Current state has fourteen local accepted pairs, custody 25, and 29 chat messages; revised readback 28 is still an unaccepted draft. Actual request 190 contains neither `pronounsOrAddress` nor the player's accepted they/them value. The initial report incorrectly attributed this response's he/his to the player: those pronouns refer to Halvard. That narration-failure claim is withdrawn. The confirmed defect is the omitted creator field in the ongoing prompt, even though opening narration carries it. Historical continuity summaries also use he/his for Quinn, but they are not independent proof that this particular narrator response misgendered the player. The disclosure assertions remain bounded to the exercised information journey.

Candidate production patch SHA256: `b5e27c9b208560725fc608b3bbaee80a33cfb75dd698d5dacbb5d10fc9d95d46`, base `a912fc96377a2825935c3e6da168b611eec5d68c`; all 666 production files verified. The combined gate passed 227 checks (`continuity-retrieval-time-final-gate.log`); final inclusive temporal wording passed the affected prompt/time checks and independent review after the gate. Requests 170–173 were first attempts, but the interpreter took 129,010 ms. Latency is reported separately from semantic correctness.

## Numeric-label retrieval repair

Lexical matching now preserves numeric and alphanumeric terms including short labels, strips surrounding punctuation while retaining internal separators, and prefers an exact normalized title when lexical scores tie. Explicit-ID priority, deadlines/dependencies, result bounds, omitted-state semantics, and authoritative custody remain unchanged. Focused tests cover 2, 23, 102, B7, punctuation, separator retention, crowded exact-title ties, and explicit-ID precedence.

A 500-pair retrieval-only probe scores exact IDs 4/4, lexical titles 4/4, and paraphrases sharing a discriminating anchor 4/4. The no-shared-anchor negative control scores 0/4. The runner now labels these categories and writes missed queries as zero instead of JSON null. This repair supports lexical recall, not general semantic similarity. Evidence: `rich-campaign-lexical-probe.json`.

An installed-browser probe of the actual exported lookup function scored twelve of twelve synthetic anchored cases, preserved the input events, and measured at most 10.2 ms per lookup. Its synthetic events were never loaded into live campaign storage and it made no provider calls. This is browser algorithm evidence, not a model-selected lookup or natural long-play recall result. Evidence: `installed-lexical-probe.json`.

Later-stage live coverage will follow actual campaign progression. V1 has no supported importer or chapter jump; changing an injected package's opening mission retains canonical identity and opening chronology without earned carryover. Such a fixture can cover an isolated mission contract but cannot establish natural transition, chronology, or reward fairness.

## Accepted identity transport and native draft correction

Ongoing narration omitted `player.pronounsOrAddress` although campaign creation stores and validates it and opening narration already receives it. The repair copies the accepted value directly into the ongoing player payload. It adds no inference, policy rewrite, or save migration. The exact parsed-payload regression failed with `undefined` before the change and passed with the accepted `they/them` value afterward. A separate reviewer approved the source and regression. The evidence correction above remains important: missing authority in the request was confirmed; the previous readback did not establish narrator misgendering.

Installed candidate patch `60104cbe98e1178fc57233a34d205ee8103e404e6985edc5e2abd1be319607d7`, based on published `3470c936601603e333cbddc0d2ece9955f6834ca`, matched all 666 production files. Exact reload preserved save, transcript, and selected alternatives. Real narration request 196 carries `player.pronounsOrAddress: they/them` from the accepted save. This proves installed transport, not universal model compliance. Evidence: `accepted-pronouns-live-wire-verified.json`.

The same handover narration repeated the retrospective timing problem: it described the earlier seated conversation as an hour ago despite accepted elapsed time of 380 seconds and the timing policy being present. The favorable earlier alternative is therefore not evidence that prompt guidance eliminated this model-quality failure. The failed draft was archived, then its one erroneous phrase was changed to “their earlier seated conversation” through native message Edit and Done. Mission, world, time, rewards, and accepted receipts stayed unchanged, with durable equality. This is a manual correction and a native edit of an unaccepted draft, not an automatic semantic repair or accepted-source rollback. Evidence: `native-handover-edit-verified.json`.

The first full gate for the identity slice stopped before visual assertions because its preview server could not bind `127.0.0.1:61396`. Investigation reproduced `EACCES`: another application held a wildcard outbound socket on that port. The harness selected a port by PID arithmetic inside the Windows dynamic range. The unchanged isolated suite passed on a different port. Its scoped repair uses the existing repository OS-assigned-port pattern; every visual assertion is unchanged and the repaired isolated suite passes. The brief close-to-child-bind race remains; this is not a race-free allocator claim. Evidence: `turn-activity-port-diagnosis.md`, `accepted-pronouns-final-gate.log`, and `accepted-pronouns-turn-activity-port-fix.log`.

The next live turn initially failed on malformed interpreter JSON and then an unsupported top-level `timeConfidence` field (197–200). Feedback preserved the semantic envelope and correctly described the first failure. Strict rejection left accepted state unchanged. One explicit Retry, 201–202, recovered; the new receipt binds edited assistant 30, swipe 0, hash `3d06fe0e`, source range `24214526`. This proves actual edited-source acceptance without reusing the original unedited text. It is separate from rollback of an already accepted source. Evidence: `native-edited-source-acceptance-verified.json`.

Through request 202 there were 157 real requests, seventeen distinct accepted source pairs, and one repeated-source replay. That continuation had sixteen local receipts, custody 27, elapsed time 420 seconds, and 33 chat messages. The handover objective had no fresh completion proposal despite accepted transfer narration. The new unaccepted staff scene also gave Priya Nayar the wrong rank. Her authored service record says Lieutenant, while the ongoing core-crew mapper supplied names and guides but omitted service/rank. The test player's own message 31 incorrectly called Nayar Commander, so this is not an isolated unprompted model-invention sample; the omitted authored rank could not correct that cue. Bronn's Tellarite species and all four core names are authored. Sato's gray hair is unsupplied detail, not a demonstrated contradiction. Rowan's they/them wording follows the current runtime dataset, which conflicts with he/him in the reviewed bible/GDD; authoring provenance needs resolution before changing that field.

The next full gate passed preview-server startup but failed the activity-card geometry assertion at 320px. Instrumentation reproduced stale placement after changing from 390px: the viewport was already 320px but the card still ended at x365. On the next animation frame, about 8ms later, its right edge was x308 and stayed within bounds. The harness now waits at most two seconds for the requested viewport and completed placement, then performs the same geometry, wrapping, and reduced-motion assertions. Both the isolated suite and a 20-transition reproduction probe passed. No production positioning or CSS changed. Evidence: `turn-activity-resize-probe.log`, `turn-activity-resize-fix.log`, and `turn-activity-resize-fixed-probe.log`.

Core crew narration guides now copy only existing typed service metadata, mirroring supporting-character guides. The exact Nayar service regression failed before the change and passes afterward. No public record, biography, People entry, presence, or knowledge authority is added. Actual narration request 205 contains all seven exact authored core service objects and the accepted player pronouns. The preserved replacement at assistant 32, swipe 1, does not mention Nayar's rank, so it establishes transport and an absence of the old error, not positive natural rank-use accuracy. Accepted mission, world, time, and receipts remained unchanged by the alternative. Final candidate patch SHA256: `d2ad372ea86b4b02831bc48285af84d7fd11e366fa53c84db533f26d951d5299`; all 666 production files verified. Evidence: `core-service-live-wire-verified.json`.

The handover trace found that the interpreter did select the valid transfer claim. A prior retry changed the player's explicit resolution intent to continue and attached unrelated participation quotes, leaving pacing authorization held. The pacing gate then discarded the otherwise valid transfer claim before reduction; simply bypassing it would not safely create a proposal. The existing native Adjust progress → Completed control was used as an explicit corrective adjudication supported by accepted assistant 30. It set the objective to completed/player_set, mission revision 5, custody 29, without adding accepted-pair evidence or changing time. Exact reload preserved the correction, all selected swipes, and durable state. This is successful manual recovery from an automatic-interpretation miss, not successful automatic extraction. Evidence: `handover-pacing-gap-analysis.md` and `handover-manual-correction-after.json`.

Final combined identity/fixture gate passed all 227 focused checks with exit 0 (`core-identity-final-gate.log`). Independent review approved the source transport and fixture changes. The two earlier gate failures and the remaining model-quality limitations remain recorded.

## Continued staff briefing and reported claims

Live batch 14 used published and installed `2ccc90b03a65ae1d6b975e4ebffa3729cd4dbac0`. Requests 206–209 accepted the selected alternative at assistant 32, swipe 1, hash `4ac800ff`, then produced initial Operations and Tactical reports. Requests 210–215 accepted assistant 34, swipe 0, hash `2108d102`, with player 35, hash `fe95a910`. These ten real requests add two distinct accepted source pairs: the cumulative totals through 215 are 170 real requests, nineteen distinct accepted source pairs, and one repeated-source replay. The minimum live coverage threshold remains unmet and does not by itself close the goal.

The first exchange completed all three analysts on their first attempts. In the second, the interpreter's first output violated duration-evidence and speaker-quote validation; its automatic retry recovered. Continuity request 212 reached its output limit after 183.707 seconds; automatic retry 214 completed after 28.078 seconds, followed by narration 215 after 48.031 seconds. No explicit Retry was needed. This is bounded automatic recovery, not a timeout or a cancellation sample.

Bronn's assertion that Cross had previously discussed a technical concern with him remains a source-bound `character-claim`, explicitly qualified by the player's request to establish the technical basis. The player's scheduling and weapons-confirmation orders remain commitments, not completed work. The manually corrected handover stays completed/player_set, while staff readiness remains incomplete. Memory and durable storage match at custody 31, mission revision 5, eighteen local accepted receipts, elapsed time 900 seconds, and 37 chat rows. Exact reload preserves state and selected swipes. Evidence: `staff-report-draft-verified.json`, `staff-delegation-verified.json`, and `staff-delegation-reload.json`.

The latest unaccepted narration gives the three remaining reports, withholds isolation-test authorization, and omits both private code phrases. Cross also reports the earlier conversation herself; that dialogue is not independent verification of historical communication. The narration repeats “Commander Nayar” despite the authored Lieutenant metadata now being supplied. The older player cue remains a confound, but transport alone plainly does not guarantee correction of generated rank errors. No universal identity or information-safety pass is claimed.

## Request latency audit through 205

The offline audit reconciles all 205 recorded request events: 45 controlled events are excluded, leaving 160 real requests. Seven real calls used for semantic replay remain in the total but are excluded from the primary latency cohort of 153. That cohort has 141 completed response bodies, ten aborted calls with terminal failure timing, and two requests without raw timing. Aborts are censored observations, not completed-response latency. All 203 available raw records join uniquely to their request events. Evidence and reproducible script: `latency-audit-through205.md`, `.json`, and `audit-latency-through205.mjs`.

| Request lane | Completed observations | Median seconds | Maximum seconds |
| --- | ---: | ---: | ---: |
| GLM 5.3 Flash interpreter | 34 | 10.828 | 129.010 |
| GLM 5.3 Flash direction | 35 | 8.281 | 294.054 |
| GLM 5.3 Flash continuity | 32 | 15.075 | 123.754 |
| Native GLM 5.2 narration | 22 | 14.580 | 27.948 |

These are finite-sample per-request measurements across the audited workload, not full-turn latency, population tail percentiles, or causal model comparisons. Fourteen narration outputs match an accepted source and hash exactly; eight are not proven accepted unchanged. Seven output-limit responses in the audited cohort had no visible content. Action-log-to-response-completion proxies are reported separately: ten successful initial sends have median 30.046 seconds and maximum 140.707 seconds; six successful explicit retries have median 39.915 seconds and maximum 112.036 seconds. Those proxies do not measure exact UI completion and cannot establish that retry caused a latency difference.

## Open manual-decision branch history defect

Production reconstruction of the saved post-correction state reproduced a timeline defect without provider calls or input mutation. An exact same-tail branch preserves the manual handover correction, as expected. Earlier inclusive assistant-20 and player-21 prefixes remove later accepted receipts and roll back time, but incorrectly retain the later completed/player_set decision at revision 5. The corresponding pre-correction snapshot reconstructs those endpoints with the earlier confirmation_required decision at revision 2.

Native branch UI on installed `2ccc90b03a65ae1d6b975e4ebffa3729cd4dbac0` then reproduced the assistant-20 cut. Child `save.1789430224686.1` has the exact 21-row prefix, ten local receipts, and elapsed time 220 seconds, yet incorrectly retains completed/player_set. Its durable state matches memory. The full 37-row parent state is preserved in checkpoint `checkpoint.1789430224686.2`, with exact transcript attestation. No provider calls were made. Evidence: `native-manual-decision-branch-reproduction.json` and its before/after captures. This child is a reproduced failure, not a passing continued timeline.

Loading that immutable parent checkpoint through the production Load Game API created fresh continuation `save.1789430305729.3`. All 37 transcript rows and selected swipes, eighteen accepted receipts, time, and objective decisions match the preserved parent. The checkpoint is unchanged and the new child's memory equals durable storage. The failed historical child remains preserved. Evidence: `staff-parent-checkpoint-restored.json`. This proves checkpoint recovery from the observed failed-result child; it does not yet prove recovery from the new refusal path being implemented.

The current objective record retains only its latest control value and no transcript position. Source invalidation removes message-backed evidence but carries the manual decision forward. Branch-time checkpoints copy the current parent state and cannot recover the missing historical value. A durable repair must preserve decisions made before the endpoint, remove later decisions, preserve ordinary edits and same-tail state, and handle legacy ambiguity truthfully. Blindly retaining or deleting every manual decision cannot satisfy that contract. This defect remains open while decision-history and correction-effect reconstruction are being designed and independently reviewed.

## Restored-timeline delegation attempt and harness correction

Batch 15 ended after eleven real calls, 216–226, without another accepted pair or narration. Cumulative totals are 181 real request events and nineteen distinct accepted source pairs. All failed attempts preserved the exact accepted state and durable equality at custody 31. The twenty-pair threshold remains unmet.

The first four calls processed actual player text `Continue.` instead of the intended detailed delegation. The harness filled the underlying composer while a native-branch naming modal remained open and later logged the requested text without verifying the posted row. Modal focus redirection plus Playwright filling an inert textarea is a supported explanation; the exact destination of the attempted insertion was not captured. No product text-clearing defect is established. The helper now refuses visible modals before composing and verifies both the composer value and actual posted source. This failed-input sample is retained and earns no delegation coverage.

Native Edit and Done changed the unaccepted player row 37 to the intended text, with source verification and accepted mission/time/rewards/receipts unchanged. Native Regenerate processed that edited source in 220–224, but continuity failed reference validation and then a 247-character quote exceeding the 240-character limit. One explicit Retry, 225–226, failed on conflicting JSON keys and then a 243-character quote. Strict rejection was correct; the batch stopped instead of repeatedly retrying. These are availability/model-conformance failures, not successful delegation or a committed partial turn. Evidence: `continue-input-failure-verified.json`, `native-player-delegation-edit.json`, `edited-delegation-validation-failure.json`, `edited-delegation-retry-failure.json`, and `batch15-closed.json`.

The subsequent source trace found three independent structural gaps (`staff-readiness-interpretation-gap.md`). Pacing context retains the latest same-mission receipt even when that receipt targets a terminal objective. Actual interpreter outputs 210/213 selected staff readiness, but deterministic pacing settlement restored the old handover objective and unresolved text. Separately, all five staff exchange policies require the handover-completed event; manually setting the handover objective to completed does not fabricate that event. Candidate construction therefore exposes no staff claims, so an empty claim list is correct within the supplied closed set even though progression is stuck.

Finally, assistant 36 contains 9,217 characters, but the accepted-pair source owner supplies only its first 7,000, cutting mid-Medical report at “That transition has”. The omitted 2,217 characters contain the substantive Medical evidence, recommendation, and five-report summary. The model's statement that the report was cut off accurately describes its packet. The configured request-context budget is 48,000 characters and the inspected interpreter prompt uses 7,565 tokens: this is a source-selection cap, not evidence that the provider context window was exhausted. These gaps remain open and require terminal-aware pacing, correction-compatible authored eligibility, and source selection that retains decisive information without inventing evidence. The later quote/JSON validation failures are separate.

## Earlier-branch history containment and installed recovery

Exact shorter native branches now refuse attachment when current or archived mission occurrences contain objective adjustments whose position cannot be reconstructed. The shared preflight runs under the timeline lease before new journal, checkpoint, or child-save writes, and direct reconstruction enforces the same rule. Full-tail branches and no-adjustment truncations retain their existing behavior. An interrupted pre-switch operation can be retired only after checking its exact ownership and active parent; switched and unrelated operations are preserved.

The native child remains available, but a specific “Earlier branch unavailable” dialog explains the limitation and offers the original campaign timeline. A child-chat metadata marker carries exact parent/child identity and the refusal reason; it grants no campaign authority. Persistence is verified by reading the saved child because native save completion alone does not acknowledge a successful write. Generation blocking is limited to the matching refused child. Tests cover ordinary-chat clearance, stale parent/child callbacks, failed marker persistence, and checkpoint recovery.

Independent review exposed and resolved persistence and stale-dialog races. Installed testing then found an additional startup delivery race: selecting the rejected child before package initialization finished restored the block but lost its dialog. Cached refusal presentation after runtime activation and UI delivery from existing deferred reconciliation now cover both paths without another analysis or authority write. A real delayed-bootstrap regression and independent re-review passed.

The initial candidate passed the 227-entry gate and actual native branching at assistant 20. The parent save, storage index, and completed journal remained unchanged; no Directive child save or checkpoint was created, and the marker was verified in the saved native chat. The final startup candidate, identity `e187893a4fe2d20ec6ea0a0ab5094112b6ab782e6e2749bd805bc785d89bce43` on base `2ccc90b`, was verified across all 667 production files. Exact parent reload preserved 38 transcript rows and all accepted state. Reopening the refused child after repeated reloads preserved its appended unaccepted draft, blocked native regeneration with zero attempted provider calls, displayed the recovery dialog even during startup, and restored the original parent through the dialog button with identical state and transcript. Native startup initially opens a system chat; the test explicitly reselects the rejected child and does not claim automatic chat reopening.

Evidence: `branch-history-live-refusal-verified.json`, `branch-refusal-appended-before-reload.json`, `branch-history-final-candidate-reload.json`, `branch-refusal-reload-proof.json`, and `branch-history-candidate-install.json`. These checks add no accepted source pairs or real provider requests: totals remain 181 real request events and nineteen distinct accepted pairs through request 226.

The updated full gate passed all 227 checks with exit code zero (`branch-history-containment-final-gate.log`), after the startup fix and independent approval. The earlier candidate's gate is retained separately as `branch-history-containment-initial-gate.log`.

This closes the demonstrated unsafe attachment path, not the complete historical-branch promise. Reconstructible correction effects, anchored objective decisions, and mission-run rollback remain open. The three staff-readiness gaps above and the broader live-soak requirements also remain open.


## Staff progression repair and batch 16

Branch containment publication was verified on GitHub at 7c274d1619f32839e1857b8e1880b6ce20db32f9. All 667 installed production files matched its Git blobs, and exact reload preserved the 38-row parent and accepted state (branch-history-published-install.json).

The next bounded slice repairs three confirmed input/progression defects. Five staff-exchange prerequisites use completed handover objective disposition, allowing manual completion without inventing a handover event. Initial and reopened states expose none of these candidates; normal automatic completion and manual completion expose the same five. Actual evidence validation/reduction tests accept five exchanges and show that reopening removes their derived events while preserving all five rejected evidence keys. Aggregate staff-readiness eligibility requires all five exchanges, and eligibility alone creates no completion event. The authored campaign suite passes 13 mission contracts and 314 scenarios.

Pacing releases terminal continue receipts to the next visible nonterminal objective, using authoritative state rather than visibility to recognize an existing open scene. It preserves current-revision leave, delegate, and skip receipts, including explicit mission departure; a later correction revokes stale departure authority. Ordinary scene leave does not authorize mission departure. All six pacing scripts pass, including the unchanged report-release and subsequent mission-transition runtime fixture.

The assistant source remains contiguous beyond its former 7,000-character prefix. Tests preserve the exact 9,217-character source and decisive Medical tail, validate its continuous quote, accept a 48,000-character request budget, and reject an 8,000-character budget before generation. Full hashes were already used, so receipt identity is unchanged and settled pairs are not retroactively reinterpreted or backfilled. Persisted working excerpts remain bounded at 240 characters and replay stays idempotent. Player-source caps are unchanged.

Independent review and full gates exposed additional pacing edges before publication: hidden current-scene report membership, current and stale mission departure, and ordinary scene-departure report release. Logs retain the failed findings/runtime checks and the interrupted gate; these are not counted as passing runs. The authored support-matrix check also caught expected source drift. Regeneration changed only five reviewed prerequisite predicates and source metadata; all six Prelude objective definitions and every controlled-corpus expectation remain unchanged. Eight Prelude corpus source hashes were refreshed. Independent comparison and the matrix test pass for 13 missions, 50 objectives, 56 cases, and 166 steps.

The initial installed staff candidate ad5891062038606c06d9233ba3c74f0176dc378d4adb19a7d87d87e29b12a30b was verified across 667 production files on base 7c274d161. Exact reload preserved the parent save save.1789430305729.3, custody 31, mission revision 5, and 38 chat rows. Batch 16 used native Regenerate on the exact preserved unaccepted player row 37; no additional player row or synthetic progression was inserted.

Actual requests 227-231 contain the complete assistant 36, exactly 9,217 characters, hash c1653512, and player 37, exactly 1,280 characters, hash ca5728fe; source-range hash is f8724ca5. The recovered Medical passage starts at offset 7,884. Interpreter requests 227 and 230 offer all five staff candidates and current staff-readiness pacing. Direction and continuity have null currentScene fields, so no equivalent pacing-payload claim is made for them. Continuity uses the recovered Medical passage. This verifies repaired inputs on the real provider wire.

The turn still failed. Interpreter 227 returned no claims and failed participation validation (an empty player quote and unauthored requirement index). Its retry 230 exhausted 8,192 completion tokens entirely in reasoning with empty content after 174.313 seconds. Direction 228 validated successfully. Continuity 229 failed thread grounding; retry 231 produced 243- and 247-character quotes against a 240-character limit. No explicit Retry was used on this candidate. Accepted state and durable storage remained exactly unchanged, with 38 rows and no narration. Batch 16 closed after five real requests and zero new accepted pairs. Cumulative totals are 186 real request events and nineteen distinct accepted pairs. Successful staff progression remains unproven.

Evidence: staff-fix-actual-wire-audit-227-231.json, staff-progression-before.json, staff-progression-failed-after.json, batch16-closed.json, and staff-progression-live-ad589-install.json. Gate failure evidence: staff-progression-first-gate-failure.log, staff-progression-interrupted-gate.log, staff-progression-report-departure-gate-failure.log, and staff-progression-matrix-drift-gate.log.

The final installed candidate 02ef52ef47c4d462c2b1123deed1eb88a726c55a94520f1d5b49d4e84d75605a adds the reviewed ordinary-departure correction. All 667 files match its selected-content/base manifest. Exact reload preserves the parent, and read-only installed production builders reproduce the full 9,217-character source, five staff candidates, and non-departing staff-readiness scene (staff-progression-final-installed-context.json). Real-provider requests 227-231 used the earlier candidate; no real-provider run on this final candidate is claimed.

The final full gate passed all 227 checks with exit code zero (staff-progression-final-gate.log) after the last pacing revision and coverage-snapshot refresh. Independent review approved all three production changes and the snapshot/corpus refresh. The slice was published as ebb010aea. Earlier failed/interrupted gates remain part of the evidence.

## Next correction-sensitive trigger boundary

A further canonical trigger gap remains open: manual handover completion cannot establish the Hesperus world fact while its runtime policy requires the absent handover event. A predicate-only adjustment would leave resolve/reopen/resolve vulnerable to reusing rejected static runtime evidence identity. The next bounded repair needs actual canonical runtime materialization, identity bound to relevant objective decisions, preserved rejection custody, and unchanged report disclosure and scene deferral. A read-only design is retained in hesperus-correction-trigger-design.md. It separately flags automatic legacy re-completion; no repair for this boundary is included in the published staff slice.

The rich 5,000-pair workload remains running; its partial progress is not a completed measurement or live-soak credit. Broader goal coverage and minimum accepted-pair/timeline thresholds remain incomplete.

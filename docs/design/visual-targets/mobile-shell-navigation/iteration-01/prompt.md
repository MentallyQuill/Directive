# Mobile Shell Navigation Prompt

Generated with the built-in image generation path as three GPT Image 2 concept directions. The generated concepts were reviewed inline; stable workspace image files were not exposed by the built-in tool for these draft concepts, so this prompt file records the durable prompt set.

## Concept A - Conservative Bottom Navigation Refinement

Use case: ui-mockup

Asset type: Directive mobile shell navigation concept art for implementation target

Primary request: Create an original UX-first LCARS-inspired Star Trek command UI concept for Directive's phone-width runtime shell inside SillyTavern. This is Concept A: conservative bottom navigation refinement.

Visual Target Unit: Mobile shell/navigation states.

Primary User Task: move between Starships, Mission, Crew, Ship, Log, and Settings on phone width while using Back when route history exists.

UX Goal: keep bottom route navigation as the primary shell model, but reduce the separate Back strip so it does not visually compete with route tabs or consume too much content height.

Required Information: Directive title, active route, Back enabled state, Close action, six route tabs, active route highlight.

Required Controls: Back, Close, Starships, Mission, Crew, Ship, Log, Settings.

Navigation: route navigation must stay on the bottom. Do not put route navigation on top. Top shell is only title/global actions. Back may be integrated into the bottom command shelf as a compact LCARS command segment.

LCARS Requirements: original LCARS-inspired geometry, dark terminal canvas, segmented amber/orange/coral/lavender/blue blocks, curved command shelf, active route as a structural LCARS segment, touch-safe controls, modern small readable labels.

Composition: phone-size extension panel, content scrolls above a single bottom command shelf that contains compact Back affordance and six route tabs without overlap; route labels remain readable.

Constraints: UX must beat aesthetic; no clipped labels, no squished icons, no overlapping bars, no official logos, no exact screenshots, no fake unreadable microtext, no decorative pseudo-buttons.

## Concept B - Rich LCARS Command Shelf

Use case: ui-mockup

Asset type: Directive mobile shell navigation concept art for implementation target

Primary request: Create an original UX-first LCARS-inspired Star Trek command UI concept for Directive's phone-width runtime shell inside SillyTavern. This is Concept B: richer LCARS command shelf.

Visual Target Unit: Mobile shell/navigation states.

Primary User Task: navigate between Directive pages on phone width and return to the previous route without a competing second bottom bar.

UX Goal: turn the bottom navigation into a strong LCARS command shelf, with Back integrated as an auxiliary segment only when route history exists.

Required Information: current route, route labels, active route, Back available state, Close action in top shell.

Required Controls: Back, Close, Starships, Mission, Crew, Ship, Log, Settings.

Navigation: bottom route navigation is mandatory and primary. No route navigation at the top. Back is a compact bottom-left auxiliary command segment, visually subordinate to route tabs.

LCARS Requirements: dark command-console canvas, thick segmented lower rail, curved end caps, amber/orange active route, lavender/blue secondary segments, high-contrast readable labels, stable touch targets, no decorative blocks that look clickable unless they are controls.

Composition: phone panel, small top title rail with close, scrollable content, a single integrated bottom shelf with a narrow Back chip and six evenly sized route segments; active route has a wide amber LCARS cap; no separate back strip above navigation.

Constraints: keep labels readable at 390px width, do not overlap body and bottom shelf, do not clip Starships or Settings labels, no official logos, no exact screenshots, no unreadable filler text.

## Concept C - Compact Implementation Target

Use case: ui-mockup

Asset type: Directive mobile shell navigation concept art for implementation target

Primary request: Create an original UX-first LCARS-inspired Star Trek command UI concept for Directive's phone-width runtime shell inside SillyTavern. This is Concept C: compact implementation target.

Visual Target Unit: Mobile shell/navigation states.

Primary User Task: operate Directive with one thumb: switch routes from the bottom, go Back when history exists, and keep content height maximized.

UX Goal: replace the current separate Back strip above bottom navigation with one compact LCARS shelf that reserves most width for route tabs and uses a narrow Back segment only when enabled.

Required Information: active route, six route labels, Back enabled/disabled state, Close in top shell.

Required Controls: Back, Close, Starships, Mission, Crew, Ship, Log, Settings.

Navigation: primary route navigation belongs on the bottom. No top route menu. Back is not a full-width separate bar; it is a compact auxiliary command at the left edge of the bottom shelf or a small docked chip overlapping the shelf edge without covering route labels.

LCARS Requirements: dark terminal canvas, one integrated segmented lower rail, curved left/right end caps, amber active route, coral Back segment, lavender/blue passive route segments, small modern fonts, stable touch targets, clear real buttons only.

Composition: 390px phone viewport; top title rail remains compact; content scroll area visibly gains height; bottom shelf height stays around one route bar plus a small integrated Back affordance; route labels fit without clipping or wrapping; active route is obvious.

Constraints: no second bottom bar, no overlapping content, no clipped labels, no oversized text, no official logos, no exact screenshots, no hidden state, no unreadable filler text.

# Settings Control Loop Prompt

Generated with the built-in image generation path as three GPT Image 2 concept directions. The generated concept images were reviewed inline; the tool did not expose stable workspace image files for these draft concepts, so this prompt file records the durable prompt set.

## Concept A - Conservative Saga-Close LCARS

Use case: ui-mockup

Asset type: Directive Settings page concept art for implementation target

Primary request: Create an original UX-first LCARS-inspired Star Trek command UI concept for Directive's Settings page, a starship command RPG extension embedded inside SillyTavern. This is Concept A: conservative Saga-close LCARS.

Visual Target Unit: Settings and State Safety page.

Primary User Task: inspect runtime status, verify save safety, review appearance packs, and run provider-assist diagnostics without hunting through a long scroll.

UX Goal: replace stacked generic cards with a compact control console that keeps current status visible and groups controls into clear route-local sub-tabs.

Required Information: active package, active save, simulation mode, storage status, theme pack, icon pack, provider assist status.

Required Controls: Verify Active Save, Settle Active State, Export Active Save, Clean Missing Records, Refresh Diagnostics, Reload Active Save, Run Provider Assist.

Navigation: primary route navigation is persistent at the bottom of the shell. Do not put route navigation on top. Page-local sub-tabs may sit inside the Settings content.

LCARS Requirements: dark terminal canvas, curved segmented rails, asymmetrical panel frames, amber/orange/coral/lavender/blue command accents, compact operational rows, touch-safe controls, clear affordance distinction between real buttons and decorative rails.

Composition: compact right-side extension panel, status strip at top, four sub-tabs labeled Systems, Safety, Appearance, Assist inside the content, dense but readable cards below.

Constraints: UX must beat aesthetic. Text must be readable, buttons must fit, no overlap, no official logos, no exact screenshots, no copyrighted interface captures, no hidden campaign facts, no unreadable filler text, no decorative panels that look clickable but do nothing.

## Concept B - Rich Operator Control-Room LCARS

Use case: ui-mockup

Asset type: Directive Settings page concept art for implementation target

Primary request: Create an original UX-first LCARS-inspired Star Trek command UI concept for Directive's Settings page, a starship command RPG extension embedded inside SillyTavern. This is Concept B: rich operator control-room LCARS.

Visual Target Unit: Settings and State Safety page.

Primary User Task: audit active runtime state, perform save safety operations, check storage health, and inspect extension appearance/provider systems.

UX Goal: make Settings feel like an operator control board with strong LCARS hierarchy while keeping every control obvious and reachable.

Required Information: package, active save, mode, storage health, save count, theme pack, icon pack, provider assist status, latest safety action.

Required Controls: Verify Active Save, Settle Active State, Export Active Save, Clean Missing Records, Refresh Diagnostics, Reload Active Save, Run Provider Assist.

Navigation: bottom route navigation is binding; do not place route navigation at the top. Inside the Settings page use compact sub-tabs or segmented controls for Systems, Safety, Appearance, and Assist.

LCARS Requirements: original LCARS-inspired geometry, dark negative space, thick segmented side rails, rounded elbow panels, amber/orange/lavender/blue accent families, dense operational rows, clear visual separation of status blocks versus command buttons.

Composition: right-side SillyTavern extension console, top in-panel status dashboard, center operator board with large Safety command group, appearance/assist panes visible as sub-tab structure, bottom route nav remains the main page switcher.

UX Constraints: modern small readable fonts, no clipped text, no overlapping buttons, controls have real affordance, decorative rails are clearly non-interactive, mobile behavior implied by compact structure.

Avoid: official Star Trek logos, exact LCARS screenshots, direct copyrighted screen copy, lorem ipsum, hidden facts, fake unreadable microtext, decorative pseudo-buttons.

## Concept C - Compact Mobile-First LCARS

Use case: ui-mockup

Asset type: Directive Settings page concept art for implementation target

Primary request: Create an original UX-first LCARS-inspired Star Trek command UI concept for Directive's Settings page, a starship command RPG extension embedded inside SillyTavern. This is Concept C: compact mobile-first LCARS, the preferred direction.

Visual Target Unit: Settings and State Safety page.

Primary User Task: use Settings on phone width to check runtime health and reach safety, appearance, and provider controls quickly.

UX Goal: avoid a long scrolling stack. Show a compact status dashboard, then page-local LCARS sub-tabs, with one pane active at a time. Keep primary route navigation persistent at the bottom.

Required Information: active package, active save, simulation mode, storage status, theme pack, icon pack, provider assist status.

Required Controls: Verify Active Save, Settle Active State, Export Active Save, Clean Missing Records, Refresh Diagnostics, Reload Active Save, Run Provider Assist.

Navigation: bottom route navigation is mandatory and prominent. No top route menu. Page-local sub-tabs live under the status dashboard, not in the top shell.

LCARS Requirements: dark terminal canvas, structural curved side rail, segmented amber/orange/lavender/blue blocks, compact rows, small modern readable text, touch-safe buttons, clear separation of navigation, status, and actions.

Composition: phone-size right-side extension panel filling the viewport, title/global actions at top, compact four-tile status grid, horizontal sub-tabs Systems / Safety / Packs / Assist, active Safety pane with action buttons in stable two-column rows, appearance and assist accessible without scrolling far, bottom route nav anchored.

UX Constraints: no clipped labels, no squished action text, no overlapping controls, no huge hero text, no official logos, no exact screenshots, no hidden state leaks, no fake unreadable filler text, no decorative panels that look clickable if they are not controls.

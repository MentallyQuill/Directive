# Sidecar Schemas

This directory owns JSON Schema contracts for model-authored sidecar outputs. Runtime code still performs deterministic validation before applying or persisting any sidecar result.

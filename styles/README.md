# Styles

Directive CSS entry files and runtime styles.

Saga currently keeps runtime rail styling in a dedicated styles path; Directive follows that convention rather than putting large CSS blocks in render modules.

`directive.css` is the manifest-loaded stylesheet for the current pre-alpha runtime shell. Keep selectors under the `directive-` prefix.

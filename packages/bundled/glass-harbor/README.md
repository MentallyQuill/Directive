# Bundled Glass Harbor Package

Bundled U.S.S. Glass Harbor campaign package and Drowned Constellation campaign data.

The normalized package record is `drowned-constellation.campaign-package.json`.

## Import Status

This package was imported from `The_Drowned_Constellation_Full_Campaign.zip` and normalized to the current schema-v2 package spine.

The `endConditions` root is authored against `docs/design/CAMPAIGN_END_CONDITIONS.md`. It covers the named Drowned finale families, terminal campaign candidates, checkpoint policy, Push On frames, final-band rules, ending-axis effects, and player-safe recovery copy.

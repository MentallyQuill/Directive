# Bundled Aster Vale Package

Bundled U.S.S. Aster Vale campaign package and Unseen Border campaign data.

The normalized package record is `unseen-border.campaign-package.json`.

## Import Status

This package was imported from `The_Unseen_Border_Full_Campaign.zip` and aligned to the current schema-v2 package spine.

The `endConditions` root is authored against `docs/design/CAMPAIGN_END_CONDITIONS.md`. It covers the named Unseen Border finale families, terminal campaign candidates, checkpoint policy, Push On frames, final-band rules, ending-axis effects, and player-safe recovery copy.

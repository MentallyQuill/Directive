# Bundled Serein Package

Bundled U.S.S. Serein campaign package and Black Current campaign data.

The normalized package record is `black-current.campaign-package.json`.

## Import Status

This package was imported from `The_Black_Current_Full_Campaign.zip` and normalized to the current schema-v2 package spine.

The `endConditions` root is authored against `docs/design/CAMPAIGN_END_CONDITIONS.md`. It covers the named Black Current finale families, terminal campaign candidates, checkpoint policy, Push On frames, final-band rules, ending-axis effects, and player-safe recovery copy.

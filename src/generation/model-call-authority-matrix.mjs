import { GENERATION_ROLE_IDS } from './generation-roles.mjs';
import { SIDECAR_OUTPUT_SCHEMA_IDS } from '../jobs/sidecar-output-contracts.mjs';

const EMPTY = Object.freeze([]);

const MATRIX = Object.freeze({
  narration: {
    roleId: 'narration',
    providerKind: 'reasoning',
    trigger: 'Committed Mission Director outcome needs player-facing prose.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/runtime/runtime-app.mjs',
    parserSchema: null,
    fallback: 'fail-retryable',
    playerVisibleOutput: 'Narration prose after mechanics commit.',
    hiddenStatePolicy: 'May use only committed/player-safe turn packets; cannot alter mechanics.',
    tests: ['test-runtime-director-turn.mjs', 'test-runtime-host-injection.mjs']
  },
  campaignIntro: {
    roleId: 'campaignIntro',
    providerKind: 'reasoning',
    trigger: 'Campaign activation posts the opening chat message.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/runtime/campaign-activation-coordinator.mjs',
    parserSchema: null,
    fallback: 'local-fallback',
    playerVisibleOutput: 'Campaign introduction prose.',
    hiddenStatePolicy: 'Uses player-safe campaign setup only.',
    tests: ['test-chat-native-activation-conclusion.mjs', 'test-chat-native-runtime-flow.mjs']
  },
  campaignConclusion: {
    roleId: 'campaignConclusion',
    providerKind: 'reasoning',
    trigger: 'Campaign conclusion recap is generated after deterministic completion.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/runtime/campaign-conclusion-service.mjs',
    parserSchema: null,
    fallback: 'local-fallback',
    playerVisibleOutput: 'Campaign conclusion prose.',
    hiddenStatePolicy: 'Uses committed conclusion facts only.',
    tests: ['test-chat-native-activation-conclusion.mjs', 'test-chat-native-runtime-flow.mjs']
  },
  missionDirectorAdvisor: {
    roleId: 'missionDirectorAdvisor',
    providerKind: 'reasoning',
    trigger: 'Player asks Directive for counsel or professional assessment outside direct in-scene dialogue.',
    blocking: false,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/runtime/chat-turn-orchestrator.mjs',
    parserSchema: null,
    fallback: 'skip',
    playerVisibleOutput: 'Structured advisory JSON for Mission, Log, and Crew surfaces; no chat prose.',
    hiddenStatePolicy: 'No mechanics, no hidden facts, no independent state authority; deterministic runtime owns any persisted advisory record.',
    tests: ['test-chat-turn-orchestrator.mjs']
  },
  missionDirectorStoryPositioner: {
    roleId: 'missionDirectorStoryPositioner',
    providerKind: 'utility',
    trigger: 'Foreground Mission Director turn needs bounded story-position candidate selection before any outcome decision.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/directors/mission-director-story-graph-spine.mjs',
    parserSchema: 'directive.storyPositionSelection.v1',
    fallback: 'fail-closed',
    playerVisibleOutput: 'None; validated story-position candidate ids and route only.',
    hiddenStatePolicy: 'Receives only player-safe source frames, visible transcript excerpts, active story projection, bounded story candidates, prompt status summaries, and recovery summaries. It cannot see raw prompts, provider reasoning, hidden truth, raw relationship values, raw pressure values, private NPC thoughts, cookies, CSRF tokens, or API keys.',
    tests: ['test-mission-director-model-spine.mjs', 'test-model-call-authority-matrix.mjs']
  },
  missionDirectorStoryPositionReviewer: {
    roleId: 'missionDirectorStoryPositionReviewer',
    providerKind: 'utility',
    trigger: 'Bounded Mission Director story-position selection requires independent source and stale-history review.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/directors/mission-director-story-graph-spine.mjs',
    parserSchema: 'directive.storyPositionReview.v1',
    fallback: 'fail-closed',
    playerVisibleOutput: 'None; approval, retry, pause, or host-continue decision only.',
    hiddenStatePolicy: 'Receives bounded candidates, selected candidate ids, active projection summaries, hashes, and player-safe source refs. It cannot see raw prompts, provider reasoning, hidden truth, raw relationship values, raw pressure values, private NPC thoughts, cookies, CSRF tokens, or API keys.',
    tests: ['test-mission-director-story-graph-spine.mjs', 'test-model-call-authority-matrix.mjs']
  },
  missionDirectorOutcomePlanner: {
    roleId: 'missionDirectorOutcomePlanner',
    providerKind: 'reasoning',
    trigger: 'Approved story position requires a bounded Mission Director outcome plan.',
    blocking: true,
    mayProposeState: true,
    mayInjectPrompt: false,
    allowedRoots: Object.freeze(['mission']),
    owningModule: 'src/directors/mission-director-model-spine.mjs',
    parserSchema: 'directive.missionOutcomePlan.v1',
    fallback: 'fail-closed',
    playerVisibleOutput: 'None directly; custody adapter validates plan before mechanics and narration packets.',
    hiddenStatePolicy: 'Receives only player-safe frame data, story position hash, allowlisted fact ids, and allowlisted decision ids. It cannot see raw prompts, provider reasoning, hidden truth, raw relationship values, raw pressure values, private NPC thoughts, cookies, CSRF tokens, or API keys.',
    tests: ['test-mission-director-model-spine.mjs', 'test-model-call-authority-matrix.mjs']
  },
  missionDirectorStoryDeltaPlanner: {
    roleId: 'missionDirectorStoryDeltaPlanner',
    providerKind: 'reasoning',
    trigger: 'Accepted Mission Director outcome plan needs append-only story ledger event drafts.',
    blocking: true,
    mayProposeState: true,
    mayInjectPrompt: false,
    allowedRoots: Object.freeze(['storyEventLedger', 'activeStoryProjection']),
    owningModule: 'src/directors/mission-director-story-graph-spine.mjs',
    parserSchema: 'directive.storyDeltaPlan.v1',
    fallback: 'fail-closed',
    playerVisibleOutput: 'None directly; custody adapter validates event drafts before appending story ledger events.',
    hiddenStatePolicy: 'Receives compact active projection, bounded story candidates, accepted selection, outcome hash, and candidate guards. It cannot see raw prompts, provider reasoning, hidden truth, raw relationship values, raw pressure values, private NPC thoughts, cookies, CSRF tokens, or API keys.',
    tests: ['test-mission-director-story-graph-spine.mjs', 'test-model-call-authority-matrix.mjs']
  },
  missionDirectorStoryDeltaReviewer: {
    roleId: 'missionDirectorStoryDeltaReviewer',
    providerKind: 'utility',
    trigger: 'Story delta event drafts require independent review before custody append.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/directors/mission-director-story-graph-spine.mjs',
    parserSchema: 'directive.storyDeltaReview.v1',
    fallback: 'fail-closed',
    playerVisibleOutput: 'None; approval, retry, pause, or host-continue decision only.',
    hiddenStatePolicy: 'Receives sanitized story delta drafts, active projection summaries, selected candidates, hashes, and safety constraints. It cannot see raw prompts, provider reasoning, hidden truth, raw relationship values, raw pressure values, private NPC thoughts, cookies, CSRF tokens, or API keys.',
    tests: ['test-mission-director-story-graph-spine.mjs', 'test-model-call-authority-matrix.mjs']
  },
  missionDirectorPlanReviewer: {
    roleId: 'missionDirectorPlanReviewer',
    providerKind: 'utility',
    trigger: 'Mission Director outcome plan requires independent model review before custody commit.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/directors/mission-director-model-spine.mjs',
    parserSchema: 'directive.missionDirectorPlanReview.v1',
    fallback: 'fail-closed',
    playerVisibleOutput: 'None; approval, retry, pause, or host-continue decision only.',
    hiddenStatePolicy: 'Receives only sanitized story position, outcome plan, hashes, and safety constraints. It cannot see raw prompts, provider reasoning, hidden truth, raw relationship values, raw pressure values, private NPC thoughts, cookies, CSRF tokens, or API keys.',
    tests: ['test-mission-director-model-spine.mjs', 'test-model-call-authority-matrix.mjs']
  },
  utilityTurnClassifier: {
    roleId: 'utilityTurnClassifier',
    providerKind: 'utility',
    trigger: 'Active campaign player message is not a deterministic fast path.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/adjudication/utility-turn-classifier.mjs',
    parserSchema: 'directive.turnIntentClassification',
    fallback: 'deterministic',
    playerVisibleOutput: 'None; validated routing decision only.',
    hiddenStatePolicy: 'Receives player-safe context; arbitration rejects hidden-state output.',
    tests: ['test-turn-intent-classifier-fixtures.mjs', 'test-chat-turn-orchestrator.mjs']
  },
  utilityTurnArbiter: {
    roleId: 'utilityTurnArbiter',
    providerKind: 'utility',
    trigger: 'Active campaign player message needs source-bound turn route and response owner arbitration.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/adjudication/utility-turn-arbiter.mjs',
    parserSchema: 'directive.turnArbiterPlan.v1',
    fallback: 'fail-closed',
    playerVisibleOutput: 'None; validated route and response ownership plan only.',
    hiddenStatePolicy: 'Receives only player-safe source frames, visible transcript excerpts, selected-swipe metadata, current mission routing summaries, prompt status summaries, and recovery summaries. It cannot see raw prompts, provider reasoning, hidden truth, raw relationship values, raw pressure values, private NPC thoughts, cookies, CSRF tokens, or API keys.',
    tests: ['test-utility-turn-arbiter.mjs', 'test-chat-turn-orchestrator.mjs']
  },
  questActionInterpreter: {
    roleId: 'questActionInterpreter',
    providerKind: 'utility',
    trigger: 'A foreground quest action must be mapped to authorized objective and method ids.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/quests/action-interpreter.mjs',
    parserSchema: 'directive.questActionInterpretation',
    fallback: 'deterministic',
    playerVisibleOutput: 'None; deterministic quest services validate and apply the chosen action.',
    hiddenStatePolicy: 'Receives only authorized ids and visible context. It cannot select results or mutate state.',
    tests: ['test-open-world-model-contracts.mjs']
  },
  questArchitect: {
    roleId: 'questArchitect',
    providerKind: 'reasoning',
    trigger: 'A grounded narrative thread qualifies for quest promotion.',
    blocking: false,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/quests/quest-architect.mjs',
    parserSchema: 'directive.questArchitectureProposal',
    fallback: 'deterministic',
    playerVisibleOutput: 'None directly; deterministic registration creates any quest template and instance.',
    hiddenStatePolicy: 'May reference only supplied facts, actors, locations, factions, assets, and source anchors. Registration is deterministic.',
    tests: ['test-open-world-dynamic-quest-e2e.mjs', 'test-open-world-model-contracts.mjs']
  },
  sceneDeltaExtractor: {
    roleId: 'sceneDeltaExtractor',
    providerKind: 'utility',
    trigger: 'A committed player/assistant exchange is converted to grounded observations.',
    blocking: false,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/threads/scene-delta-extractor.mjs',
    parserSchema: 'directive.sceneDelta',
    fallback: 'deterministic',
    playerVisibleOutput: 'None directly; extracted observations remain evidence until deterministic processors apply them.',
    hiddenStatePolicy: 'Every observation requires a source message id or quoted evidence. It cannot commit state.',
    tests: ['test-open-world-dynamic-quest-e2e.mjs', 'test-open-world-model-contracts.mjs']
  },
  sceneReconciliationExtractor: {
    roleId: 'sceneReconciliationExtractor',
    providerKind: 'utility',
    trigger: 'Changed chat chunks require bounded reconciliation proposals.',
    blocking: false,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/runtime/scene-reconciliation.mjs',
    parserSchema: 'directive.sceneReconciliationObservations',
    fallback: 'deterministic',
    playerVisibleOutput: 'None directly; accepted proposals still pass state validation and revision checks.',
    hiddenStatePolicy: 'Chat text is evidence only. Accepted proposals still pass state validation and revision checks.',
    tests: ['test-scene-reconciliation.mjs', 'test-scene-reconciliation-open-world.mjs', 'test-open-world-model-contracts.mjs']
  },
  sceneHandshakeSettler: {
    roleId: 'sceneHandshakeSettler',
    providerKind: 'utility',
    trigger: 'A player reply may accept the immediately previous host-generated assistant response as current fiction.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/runtime/scene-handshake-settler.mjs',
    parserSchema: 'directive.sceneHandshakeSettlement.v1',
    fallback: 'defer',
    playerVisibleOutput: 'None directly; deterministic validation commits only allowlisted player-safe records.',
    hiddenStatePolicy: 'Receives a compact player-safe two-message snapshot plus hashes, time, location, and referenced resolver ids. It cannot write raw state deltas; the runtime validator owns all commits.',
    tests: ['test-scene-handshake-settler.mjs', 'test-chat-turn-orchestrator.mjs']
  },
  sourceSettlementLatestPair: {
    roleId: 'sourceSettlementLatestPair',
    providerKind: 'utility',
    trigger: 'SRE owns latest-pair settlement for a clean player reply to the immediately previous assistant message.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/runtime/scene-handshake-settler.mjs',
    parserSchema: 'directive.sceneHandshakeSettlement.v1',
    fallback: 'fail-closed',
    playerVisibleOutput: 'None directly; SRE validates source cleanliness and the Scene Handshake validator applies allowlisted operations.',
    hiddenStatePolicy: 'Receives the same compact player-safe two-message snapshot as Scene Handshake plus source hashes. Durable SRE diagnostics keep hashes and metadata, not raw prompt, player, or assistant text.',
    tests: ['test-chat-turn-orchestrator.mjs', 'test-source-reconciliation-engine-synthetic.mjs', 'test-model-call-authority-matrix.mjs']
  },
  timeAdvanceAdjudicator: {
    roleId: 'timeAdvanceAdjudicator',
    providerKind: 'utility',
    trigger: 'Accepted scene text implies ambiguous elapsed time that deterministic rules cannot confidently resolve.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/time/time-advance-adjudicator.mjs',
    parserSchema: 'directive.timeAdvanceProposal.v1',
    fallback: 'deterministic',
    playerVisibleOutput: 'None directly; deterministic runtime validates, clamps, and commits any approved time boundary.',
    hiddenStatePolicy: 'Receives visible source text, source hashes, and current clock summary only. It cannot see hidden clocks, hidden pressure values, prompts, provider reasoning, cookies, CSRF tokens, API keys, or raw campaign state.',
    tests: ['test-time-advance-adjudicator.mjs', 'test-scene-handshake-settler.mjs', 'test-model-call-authority-matrix.mjs']
  },
  relationshipEvaluator: {
    roleId: 'relationshipEvaluator',
    providerKind: 'utility',
    trigger: 'Committed turn may affect crew relationships.',
    blocking: false,
    mayProposeState: true,
    mayInjectPrompt: false,
    allowedRoots: Object.freeze(['relationships', 'crew']),
    owningModule: 'src/jobs/campaign-sidecar-scheduler.mjs',
    parserSchema: SIDECAR_OUTPUT_SCHEMA_IDS.stateDeltaProposal,
    fallback: 'journal-only',
    playerVisibleOutput: 'None directly; proposal-only state deltas.',
    hiddenStatePolicy: 'Proposal roots are allowlisted and revalidated by the state gateway.',
    tests: ['test-campaign-sidecar-scheduler.mjs']
  },
  commandBearingFitChecker: {
    roleId: 'commandBearingFitChecker',
    providerKind: 'utility',
    trigger: 'Player asks Assist to check whether the current draft fits Inspiration or Resolve.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/command/command-bearing-fit.mjs',
    parserSchema: 'directive.commandBearingFitCheck',
    fallback: 'deterministic',
    playerVisibleOutput: 'A brief fit report and tips; never replacement prose.',
    hiddenStatePolicy: 'Receives player-safe context only. Cannot write state, award Marks, spend points, or revise player text.',
    tests: ['test-directive-assist.mjs', 'test-model-call-authority-matrix.mjs']
  },
  commandBearingSpendValidator: {
    roleId: 'commandBearingSpendValidator',
    providerKind: 'utility',
    trigger: 'A Readied Inspiration or Resolve point is attached to the exact sent player message.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/runtime/chat-turn-orchestrator.mjs',
    parserSchema: 'directive.commandBearingFitCheck',
    fallback: 'fail-closed',
    playerVisibleOutput: 'None directly; invalid or failed validation returns the Readied point before commit.',
    hiddenStatePolicy: 'Receives player-safe turn context and returns only fit, validity, summary, and causal basis. Deterministic spend validation remains authoritative.',
    tests: ['test-chat-turn-orchestrator.mjs', 'test-command-bearing.mjs']
  },
  commandBearingEvaluator: {
    roleId: 'commandBearingEvaluator',
    providerKind: 'utility',
    trigger: 'Committed turn may contain Command Bearing evidence or review signal.',
    blocking: false,
    mayProposeState: true,
    mayInjectPrompt: false,
    allowedRoots: Object.freeze(['commandBearing', 'commandCulture']),
    owningModule: 'src/jobs/campaign-sidecar-scheduler.mjs',
    parserSchema: SIDECAR_OUTPUT_SCHEMA_IDS.stateDeltaProposal,
    fallback: 'journal-only',
    playerVisibleOutput: 'None directly; Command Bearing proposals must pass deterministic validators and player-safe projections.',
    hiddenStatePolicy: 'May append/upsert validated Command Bearing evidence and propose command-culture observations only. Generic sidecars cannot write review ledgers, Marks, points, spends, readied state, or recovery.',
    tests: ['test-campaign-sidecar-scheduler.mjs', 'test-command-bearing.mjs']
  },
  outcomeIntegrityReview: {
    roleId: 'outcomeIntegrityReview',
    providerKind: 'utility',
    trigger: 'Player edits protected Directive-owned assistant prose while Outcome Integrity requires review.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/runtime/outcome-integrity.mjs',
    parserSchema: 'directive.outcomeIntegrityReview.v1',
    fallback: 'fail-closed',
    playerVisibleOutput: 'Edit acceptance, rejection, or repair guidance for protected assistant prose.',
    hiddenStatePolicy: 'May compare proposed prose with committed/player-safe outcome context only. It cannot alter mechanics, costs, Command Bearing, relationships, or campaign state.',
    tests: ['test-model-call-authority-matrix.mjs']
  },
  promptContextBuilder: {
    roleId: 'promptContextBuilder',
    providerKind: 'utility',
    trigger: 'Prompt context needs compact player-safe assistance.',
    blocking: false,
    mayProposeState: false,
    mayInjectPrompt: true,
    allowedRoots: EMPTY,
    owningModule: 'src/generation/player-safe-prompt-context-builder.mjs',
    parserSchema: null,
    fallback: 'deterministic',
    playerVisibleOutput: 'None; host prompt blocks only.',
    hiddenStatePolicy: 'Prompt builder is allowlisted and player-safe.',
    tests: ['test-player-safe-prompt-context.mjs', 'test-prompt-injection-safety.mjs']
  },
  continuityProjectionPlanner: {
    roleId: 'continuityProjectionPlanner',
    providerKind: 'utility',
    trigger: 'Continuity Projection Matrix needs a bounded fact-id projection plan before the next prompt packet is installed.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/continuity/',
    parserSchema: 'directive.continuityProjectionPlan.v1',
    fallback: 'last-good-then-deterministic',
    playerVisibleOutput: 'None; selected fact ids and lane preferences only.',
    hiddenStatePolicy: 'Receives audience-filtered candidate facts. It cannot author facts, mutate campaign state, reveal hidden data, or install prompt keys.',
    tests: ['test-continuity-projection-foundation.mjs', 'test-continuity-projection-plan-validator.mjs', 'test-continuity-projection-planner-client.mjs', 'test-model-call-authority-matrix.mjs']
  },
  continuityContradictionReviewer: {
    roleId: 'continuityContradictionReviewer',
    providerKind: 'utility',
    trigger: 'Generated prose may contain high-risk claims that must be checked against projected continuity before dispatch.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/continuity/',
    parserSchema: 'directive.continuityContradictionReview.v1',
    fallback: 'fail-closed',
    playerVisibleOutput: 'None; retry or repair decision only.',
    hiddenStatePolicy: 'Receives generated text plus relevant narrator-safe facts. It cannot commit facts, alter outcomes, or expose hidden records.',
    tests: ['test-continuity-projection-foundation.mjs', 'test-model-call-authority-matrix.mjs']
  },
  continuityClaimExtractor: {
    roleId: 'continuityClaimExtractor',
    providerKind: 'utility',
    trigger: 'Committed or rejected generated prose needs high-risk factual claims extracted for quarantine and later validation.',
    blocking: false,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/continuity/',
    parserSchema: 'directive.continuityClaimExtraction.v1',
    fallback: 'skip',
    playerVisibleOutput: 'None; candidate claims remain non-authoritative until deterministic validation accepts them.',
    hiddenStatePolicy: 'Generated text is evidence only. Extracted claims are quarantined and cannot directly become campaign facts.',
    tests: ['test-continuity-projection-foundation.mjs', 'test-model-call-authority-matrix.mjs']
  },
  continuityProjectionCompressor: {
    roleId: 'continuityProjectionCompressor',
    providerKind: 'utility',
    trigger: 'Lower-priority continuity facts exceed the revolving context budget and need loss-aware summarization.',
    blocking: false,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/continuity/',
    parserSchema: 'directive.continuityProjectionCompression.v1',
    fallback: 'deterministic',
    playerVisibleOutput: 'None; compressed context proposals only.',
    hiddenStatePolicy: 'Receives already audience-filtered facts and cannot promote, invent, or reveal facts.',
    tests: ['test-continuity-projection-foundation.mjs', 'test-model-call-authority-matrix.mjs']
  },
  continuityTracker: {
    roleId: 'continuityTracker',
    providerKind: 'utility',
    trigger: 'Committed turn may require continuity notes or known-fact cleanup.',
    blocking: false,
    mayProposeState: true,
    mayInjectPrompt: false,
    allowedRoots: Object.freeze(['continuity', 'mission']),
    owningModule: 'src/jobs/campaign-sidecar-scheduler.mjs',
    parserSchema: SIDECAR_OUTPUT_SCHEMA_IDS.stateDeltaProposal,
    fallback: 'journal-only',
    playerVisibleOutput: 'None directly; proposal-only continuity deltas.',
    hiddenStatePolicy: 'Allowed roots only; stale revisions are rejected.',
    tests: ['test-campaign-sidecar-scheduler.mjs']
  },
  crewDirector: {
    roleId: 'crewDirector',
    providerKind: 'utility',
    trigger: 'Committed turn may affect crew condition or assignments.',
    blocking: false,
    mayProposeState: true,
    mayInjectPrompt: false,
    allowedRoots: Object.freeze(['crew']),
    owningModule: 'src/jobs/campaign-sidecar-scheduler.mjs',
    parserSchema: SIDECAR_OUTPUT_SCHEMA_IDS.stateDeltaProposal,
    fallback: 'journal-only',
    playerVisibleOutput: 'None directly; proposal-only crew deltas.',
    hiddenStatePolicy: 'Allowed crew root only; no direct narration authority.',
    tests: ['test-campaign-sidecar-scheduler.mjs']
  },
  shipDirector: {
    roleId: 'shipDirector',
    providerKind: 'utility',
    trigger: 'Committed turn may affect ship condition or systems.',
    blocking: false,
    mayProposeState: true,
    mayInjectPrompt: false,
    allowedRoots: Object.freeze(['ship']),
    owningModule: 'src/jobs/campaign-sidecar-scheduler.mjs',
    parserSchema: SIDECAR_OUTPUT_SCHEMA_IDS.stateDeltaProposal,
    fallback: 'journal-only',
    playerVisibleOutput: 'None directly; proposal-only ship deltas.',
    hiddenStatePolicy: 'Allowed ship root only; no mechanics authority.',
    tests: ['test-campaign-sidecar-scheduler.mjs']
  },
  commandLogSummarizer: {
    roleId: 'commandLogSummarizer',
    providerKind: 'utility',
    trigger: 'Committed Command Log entry needs compact player-facing summary.',
    blocking: false,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/jobs/command-log-summary-sidecar.mjs',
    parserSchema: SIDECAR_OUTPUT_SCHEMA_IDS.commandLogSummary,
    fallback: 'skip',
    playerVisibleOutput: 'Command Log assisted summary.',
    hiddenStatePolicy: 'Committed player-visible state only; schema rejects hidden-state language.',
    tests: ['test-command-log-summary-sidecar.mjs']
  },
  recapSummarizer: {
    roleId: 'recapSummarizer',
    providerKind: 'utility',
    trigger: 'Compact recap generation.',
    blocking: false,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/runtime/campaign-conclusion-service.mjs',
    parserSchema: null,
    fallback: 'defer',
    playerVisibleOutput: 'Player-facing recap text or structured recap.',
    hiddenStatePolicy: 'Committed/player-safe state only.',
    tests: ['test-chat-native-activation-conclusion.mjs']
  },
  factualGroundingReviewer: {
    roleId: 'factualGroundingReviewer',
    providerKind: 'utility',
    trigger: 'Live campaign soak asks a model to review visible transcript excerpts against player-safe factual canaries.',
    blocking: false,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/runtime/runtime-app.mjs',
    parserSchema: 'directive.liveCampaignSoak.factualModelReviewResult',
    fallback: 'skip',
    playerVisibleOutput: 'None directly; test artifacts only.',
    hiddenStatePolicy: 'Receives only player-safe canaries, source pointers, deterministic summaries, and visible transcript excerpts. It cannot see raw prompts, provider reasoning, hidden state, raw relationship values, hidden pressures, hidden clocks, CSRF tokens, cookies, or API keys.',
    tests: ['test-generation-router.mjs', 'test-runtime-host-injection.mjs', 'test-live-soak-prep.mjs']
  },
  storyQualityReviewer: {
    roleId: 'storyQualityReviewer',
    providerKind: 'utility',
    trigger: 'Live campaign soak asks a model to review visible transcript excerpts for story quality, tense/PoV, agency, continuity, and prose quality.',
    blocking: false,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/runtime/runtime-app.mjs',
    parserSchema: 'directive.liveCampaignSoak.storyQualityModelReviewResult',
    fallback: 'skip',
    playerVisibleOutput: 'None directly; test artifacts only.',
    hiddenStatePolicy: 'Receives only visible transcript excerpts, deterministic score summaries, score definitions, and player-safe artifact pointers. It cannot see raw prompts, provider reasoning, hidden state, raw relationship values, hidden pressures, hidden clocks, CSRF tokens, cookies, or API keys.',
    tests: ['test-generation-router.mjs', 'test-runtime-host-injection.mjs', 'test-live-soak-prep.mjs']
  },
  defineSelection: {
    roleId: 'defineSelection',
    providerKind: 'utility',
    trigger: 'Player highlights visible chat text and asks Define Selection for in-universe context.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/runtime/define-selection.mjs',
    parserSchema: 'directive.defineSelection.result.v1',
    fallback: 'deterministic',
    playerVisibleOutput: 'Read-only pop-up context explanation for highlighted text.',
    hiddenStatePolicy: 'Receives selected visible text, source/current scene windows, and player-safe indexes only. It cannot mutate campaign state, save Mission Components, reveal hidden state, use discarded swipes, or expose raw diagnostics.',
    tests: ['test-define-selection.mjs', 'test-mission-components-capture.mjs', 'test-model-call-authority-matrix.mjs']
  },
  directiveAssist: {
    roleId: 'directiveAssist',
    providerKind: 'reasoning',
    trigger: 'User invokes Directive Assist rewrite/inspection.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/assist/directive-assist.mjs',
    parserSchema: 'directive.assistResponse',
    fallback: 'fail-retryable',
    playerVisibleOutput: 'Assist replacement text and warnings.',
    hiddenStatePolicy: 'No campaign hidden state authority.',
    tests: ['test-directive-assist.mjs']
  },
  characterCreatorSectionDraft: {
    roleId: 'characterCreatorSectionDraft',
    providerKind: 'reasoning',
    trigger: 'Character Creator requests a section draft.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/creators/character-creator-assist.mjs',
    parserSchema: 'directive.characterCreatorSectionDraft',
    fallback: 'local-fallback',
    playerVisibleOutput: 'Creator draft text.',
    hiddenStatePolicy: 'No gameplay hidden state; draft output only.',
    tests: ['test-character-creator-assist.mjs']
  },
  utilityJson: {
    roleId: 'utilityJson',
    providerKind: 'utility',
    trigger: 'Generic low-cost structured JSON helper.',
    blocking: true,
    mayProposeState: false,
    mayInjectPrompt: false,
    allowedRoots: EMPTY,
    owningModule: 'src/hosts/sillytavern/provider-client.mjs',
    parserSchema: null,
    fallback: 'fail-retryable',
    playerVisibleOutput: 'Caller-owned structured output.',
    hiddenStatePolicy: 'Caller must supply player-safe input and validate output.',
    tests: ['test-directive-provider-routing.mjs', 'test-sillytavern-generation-client.mjs']
  }
});

export const MODEL_CALL_AUTHORITY_MATRIX = MATRIX;

export function authorityForRole(roleId) {
  const entry = MATRIX[roleId];
  if (!entry) throw new Error(`Unknown model-call authority role "${roleId}".`);
  return {
    ...entry,
    allowedRoots: [...entry.allowedRoots],
    tests: [...entry.tests]
  };
}

export function listModelCallAuthorityMatrix() {
  return GENERATION_ROLE_IDS.map(authorityForRole);
}

export function allowedRootsForModelRole(roleId) {
  return authorityForRole(roleId).allowedRoots;
}

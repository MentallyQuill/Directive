# Package Source

Runtime package schema adapters, package loading, package library records, bundled-package discovery, and import/export helpers.

`starship-package-context.mjs` is the first pure adapter. It turns validated package JSON into Starships-tab summary data and package-driven Character Creator context without mutating package templates.

# Provider Source

Provider-independent response parsing, structured-output normalization, and exact Utility/Reasoning lane settings.

Host-specific requests and secret handling live under `src/hosts/<host>/`. Provider output is untrusted until the consuming V1 contract validates it.

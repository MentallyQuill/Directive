# Extension Source

Thin SillyTavern manifest entrypoint, lifecycle exports, runtime mount, and global bridge. Production chat, generation, storage, prompt, activation, and the single composer launcher belong to `src/hosts/sillytavern/`.
